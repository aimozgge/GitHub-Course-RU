# Урок 3.1: Создание и переключение веток (branches)

## 🎯 Цель урока
Научиться работать с ветками: создавать, переключаться между ними, понимать зачем они нужны.

## 📚 Термины урока

**Branch** (EN) → **Ветка** (RU) → Отдельная линия разработки. Позволяет работать над разными функциями параллельно.

**Checkout** (EN) → **Переключение** (RU) → Переход на другую ветку. Команда: `git checkout` или `git switch`.

**Main/Master** (EN) → **Главная ветка** (RU) → Основная ветка проекта, где хранится стабильная версия.

## 📋 Шаг 1: Зачем нужны ветки?

**Проблема без веток:**
- Вы работаете над новой функцией
- Вдруг нужно срочно исправить баг
- Приходится откатывать изменения или копировать файлы

**Решение с ветками:**
- `main` — стабильная версия
- `feature-new-analysis` — новая функция
- `bugfix-calculate-error` — исправление бага

Можно переключаться между ветками и работать параллельно!

## 📋 Шаг 2: Просмотр веток

```bash
# Показать все локальные ветки
git branch

# Показать все ветки (включая удаленные)
git branch -a
```

**Результат:**
```
* main
```

`*` означает текущую ветку.

## 📋 Шаг 3: Создание новой ветки

### Способ 1: Создать и переключиться сразу

```bash
# Создать новую ветку и переключиться на неё
git checkout -b feature-new-function

# Или (современный способ)
git switch -c feature-new-function
```

### Способ 2: Создать, затем переключиться

```bash
# Создать ветку
git branch feature-new-function

# Переключиться на неё
git checkout feature-new-function

# Или
git switch feature-new-function
```

### Через интерфейс Cursor:

1. Откройте Source Control
2. Нажмите на название ветки (внизу слева, обычно `main`)
3. Выберите "Create new branch"
4. Введите имя: `feature-new-function`

## 📋 Шаг 4: Проверка текущей ветки

```bash
# Показать текущую ветку
git branch

# Или
git status
```

**В статусе будет:**
```
On branch feature-new-function
```

## 📋 Шаг 5: Работа в новой ветке

После переключения на новую ветку:

1. Внесите изменения в файлы
2. Создайте коммиты как обычно

**Пример:**
```bash
# Вы на ветке feature-new-function
echo "Новая функция" > new_feature.py
git add new_feature.py
git commit -m "Добавлена новая функция"
```

**Важно:** Коммиты в этой ветке не влияют на `main`!

## 📋 Шаг 6: Переключение между ветками

```bash
# Переключиться на main
git checkout main

# Или
git switch main

# Переключиться обратно
git switch feature-new-function
```

**Что происходит:**
- Git меняет файлы в рабочей директории
- Показывает версию файлов из выбранной ветки

## 📋 Шаг 7: Именование веток (конвенции)

### Рекомендуемые имена:

- `feature-*` — новая функция
  - `feature-data-analysis`
  - `feature-user-auth`
  
- `bugfix-*` — исправление бага
  - `bugfix-calculation-error`
  - `bugfix-import-issue`
  
- `hotfix-*` — срочное исправление
  - `hotfix-security-patch`
  
- `experiment-*` — эксперименты
  - `experiment-new-algorithm`

**Правила:**
- Используйте маленькие буквы
- Разделяйте слова дефисом `-`
- Будьте описательными

## 📋 Шаг 8: Просмотр различий между ветками

```bash
# Показать различия между ветками
git diff main..feature-new-function

# Показать коммиты, которых нет в main
git log main..feature-new-function
```

## 📋 Шаг 9: Удаление ветки

```bash
# Удалить ветку (после слияния)
git branch -d feature-new-function

# Принудительное удаление (если не слита)
git branch -D feature-new-function
```

**Важно:** Нельзя удалить ветку, на которой вы находитесь!

## 📋 Шаг 10: Практический пример

```bash
# 1. Начать с main
git checkout main

# 2. Создать ветку для новой функции
git checkout -b feature-data-visualization

# 3. Добавить новый файл
echo "import matplotlib.pyplot as plt" > visualization.py
git add visualization.py
git commit -m "Добавлен модуль визуализации"

# 4. Вернуться на main
git checkout main

# 5. main не содержит visualization.py!
# 6. Вернуться на feature ветку
git checkout feature-data-visualization
# visualization.py снова здесь!
```

## ✅ Проверка знаний

1. Создайте ветку `feature-test`
2. Добавьте файл `test.py` и сделайте коммит
3. Переключитесь на `main` и убедитесь, что `test.py` нет
4. Вернитесь на `feature-test`

## 🎓 Что вы узнали

- ✅ Зачем нужны ветки
- ✅ Как создавать ветки
- ✅ Как переключаться между ветками
- ✅ Как именовать ветки
- ✅ Как удалять ветки

## ➡️ Следующий шаг

Переходите к **Уроку 3.2: Слияние веток (merge)**

---

**💡 Совет:** Используйте ветки для каждой новой функции или исправления. Это поможет сохранить `main` стабильной!
