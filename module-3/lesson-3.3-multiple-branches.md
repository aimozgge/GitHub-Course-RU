# Урок 3.3: Работа с несколькими ветками

## 🎯 Цель урока
Научиться эффективно работать с несколькими ветками одновременно, переключаться между ними, управлять удаленными ветками.

## 📚 Термины урока

**Remote Branch** (EN) → **Удаленная ветка** (RU) → Ветка, которая существует на GitHub, но может отсутствовать локально.

**Tracking Branch** (EN) → **Отслеживаемая ветка** (RU) → Локальная ветка, связанная с удаленной веткой на GitHub.

**Upstream** (EN) → **Вверх по течению** (RU) → Удаленная ветка, с которой синхронизируется локальная.

## 📋 Шаг 1: Сценарий работы с несколькими ветками

**Типичная ситуация:**
- `main` — стабильная версия
- `feature-analysis` — новая функция анализа
- `bugfix-calculation` — исправление ошибки в расчетах
- `experiment-visualization` — эксперимент с визуализацией

Нужно уметь переключаться между ними и синхронизировать с GitHub.

## 📋 Шаг 2: Просмотр всех веток

```bash
# Локальные ветки
git branch

# Все ветки (локальные + удаленные)
git branch -a

# Ветки с информацией о синхронизации
git branch -vv
```

**Результат:**
```
  bugfix-calculation
  experiment-visualization
* feature-analysis        abc123 [origin/feature-analysis: ahead 2]
  main                    def456 [origin/main: up to date]
```

`*` — текущая ветка

## 📋 Шаг 3: Работа с удаленными ветками

### Получить информацию об удаленных ветках:

```bash
# Загрузить информацию о ветках на GitHub
git fetch origin

# Показать удаленные ветки
git branch -r
```

### Создать локальную ветку из удаленной:

```bash
# Создать локальную ветку, отслеживающую удаленную
git checkout -b feature-analysis origin/feature-analysis

# Или (современный способ)
git switch -c feature-analysis origin/feature-analysis

# Или автоматически (Git сам создаст локальную ветку)
git checkout feature-analysis
```

## 📋 Шаг 4: Отправка ветки на GitHub

```bash
# Отправить текущую ветку на GitHub
git push origin feature-analysis

# При первом push установить связь с удаленной веткой
git push -u origin feature-analysis

# После этого можно просто использовать
git push
```

## 📋 Шаг 5: Переключение между ветками (практика)

```bash
# Список веток
git branch

# Переключиться на другую ветку
git switch feature-analysis

# Работать над изменениями
# ... редактируете файлы ...
git add .
git commit -m "Изменения в feature-analysis"

# Переключиться на другую ветку
git switch bugfix-calculation

# Git автоматически сохранит ваши изменения
# и покажет версию файлов из bugfix-calculation
```

**Важно:** Git не позволит переключиться, если есть незакоммиченные изменения (если они конфликтуют).

## 📋 Шаг 6: Stash — временное сохранение изменений

Если нужно переключиться, но изменения еще не готовы для коммита:

```bash
# Сохранить изменения временно
git stash

# Переключиться на другую ветку
git switch bugfix-calculation

# Поработать там
# ... изменения ...
git commit -m "Исправлен баг"

# Вернуться обратно
git switch feature-analysis

# Вернуть сохраненные изменения
git stash pop
```

**Просмотр stash:**
```bash
git stash list
```

## 📋 Шаг 7: Синхронизация нескольких веток

### Сценарий: Работа над несколькими функциями

```bash
# 1. Начать с обновления main
git switch main
git pull origin main

# 2. Переключиться на feature-analysis
git switch feature-analysis
git pull origin feature-analysis  # если ветка на GitHub

# 3. Внести изменения
# ... работа ...
git add .
git commit -m "Улучшена функция анализа"
git push origin feature-analysis

# 4. Переключиться на bugfix-calculation
git switch bugfix-calculation
git pull origin bugfix-calculation

# 5. Работать над исправлением
# ... изменения ...
git add .
git commit -m "Исправлена ошибка"
git push origin bugfix-calculation
```

## 📋 Шаг 8: Удаление веток

### Удалить локальную ветку:

```bash
# Удалить слитую ветку
git branch -d feature-analysis

# Принудительное удаление
git branch -D feature-analysis
```

### Удалить удаленную ветку:

```bash
# Удалить ветку на GitHub
git push origin --delete feature-analysis

# Или
git push origin :feature-analysis
```

## 📋 Шаг 9: Переименование ветки

```bash
# Переименовать текущую ветку
git branch -m new-branch-name

# Переименовать другую ветку
git branch -m old-name new-name

# Если ветка уже на GitHub, нужно:
# 1. Переименовать локально
git branch -m old-name new-name

# 2. Удалить старую на GitHub
git push origin --delete old-name

# 3. Отправить новую
git push -u origin new-name
```

## 📋 Шаг 10: Практический пример workflow

```bash
# День 1: Создание веток для разных задач
git checkout main
git checkout -b feature-data-loading
git checkout -b bugfix-import-error

# День 2: Работа над feature
git checkout feature-data-loading
# ... работа ...
git add .
git commit -m "Добавлена загрузка данных"
git push -u origin feature-data-loading

# День 3: Срочное исправление бага
git checkout bugfix-import-error
# ... исправление ...
git add .
git commit -m "Исправлена ошибка импорта"
git push -u origin bugfix-import-error

# День 4: Слияние исправления в main
git checkout main
git merge bugfix-import-error
git push origin main

# День 5: Продолжение работы над feature
git checkout feature-data-loading
git pull origin feature-data-loading  # на случай изменений
# ... продолжаем работу ...
```

## ✅ Проверка знаний

1. Создайте 3 ветки: `feature-a`, `feature-b`, `bugfix-c`
2. В каждой ветке создайте файл и сделайте коммит
3. Отправьте все ветки на GitHub
4. Переключайтесь между ветками и проверяйте файлы
5. Удалите одну из веток

## 🎓 Что вы узнали

- ✅ Как работать с несколькими ветками
- ✅ Как синхронизировать ветки с GitHub
- ✅ Как использовать stash для временного сохранения
- ✅ Как удалять и переименовывать ветки
- ✅ Типичный workflow работы с ветками

## ➡️ Следующий шаг

Переходите к **Практике 3: Создание feature-ветки для новой функции**

---

**💡 Совет:** Используйте понятные имена веток. Это поможет не запутаться при работе с несколькими задачами одновременно!
