# Урок 3.2: Слияние веток (merge)

## 🎯 Цель урока
Научиться объединять изменения из разных веток в одну, понимать процесс слияния.

## 📚 Термины урока

**Merge** (EN) → **Слияние** (RU) → Объединение изменений из одной ветки в другую.

**Fast-forward** (EN) → **Быстрая перемотка** (RU) → Тип слияния, когда просто перемещается указатель ветки (нет конфликтов).

**Merge Commit** (EN) → **Коммит слияния** (RU) → Специальный коммит, создаваемый при слиянии веток.

## 📋 Шаг 1: Зачем нужно слияние?

**Сценарий:**
1. Вы создали ветку `feature-new-analysis`
2. Добавили новую функцию
3. Протестировали — всё работает
4. Теперь нужно добавить эту функцию в `main`

**Решение:** Слить ветку `feature-new-analysis` в `main`

## 📋 Шаг 2: Процесс слияния (общий)

```
main:              A---B---C
                        \
feature-branch:          D---E
                            \
main (после merge):      A---B---C---F
                                    (F = merge D+E)
```

## 📋 Шаг 3: Слияние ветки в main

### Шаг 3.1: Убедиться, что всё закоммичено

```bash
# Проверить статус
git status

# Если есть незакоммиченные изменения, закоммитить
git add .
git commit -m "Завершена работа над функцией"
```

### Шаг 3.2: Переключиться на main

```bash
git checkout main
# Или
git switch main
```

### Шаг 3.3: Обновить main (если нужно)

```bash
# Получить последние изменения с GitHub
git pull origin main
```

### Шаг 3.4: Выполнить слияние

```bash
# Слить feature-branch в main
git merge feature-new-analysis
```

**Результат при успешном слиянии:**
```
Updating abc123..def456
Fast-forward
 new_feature.py | 10 ++++++++++
 1 file changed, 10 insertions(+)
 create mode 100644 new_feature.py
```

## 📋 Шаг 4: Типы слияния

### Fast-forward merge (быстрая перемотка)

**Когда:** В `main` не было новых коммитов после создания ветки.

```
main:        A---B
                  \
feature:           C---D
                        \
main (после):      A---B---C---D  (просто переместили указатель)
```

**Команда:**
```bash
git merge feature-branch
```

### Merge commit (коммит слияния)

**Когда:** В `main` были новые коммиты.

```
main:        A---B---E
                  \
feature:           C---D
                        \
main (после):      A---B---E---F
                        \     /
                         C---D
```

**Команда:**
```bash
git merge feature-branch
# Git создаст коммит F, который объединяет E и D
```

## 📋 Шаг 5: Слияние через интерфейс Cursor

1. Переключитесь на `main` (через Source Control)
2. Нажмите `...` (More Actions)
3. Выберите **"Branch" → "Merge Branch"**
4. Выберите ветку для слияния (например, `feature-new-analysis`)
5. Подтвердите слияние

## 📋 Шаг 6: Проверка результата слияния

```bash
# Показать историю
git log --oneline --graph

# Результат:
*   abc123 Merge branch 'feature-new-analysis'
|\
| * def456 Добавлена новая функция
* | ghi789 Обновлен README
|/
* jkl012 Начальный коммит
```

**Визуализация:**
- `*` — коммит
- `|` — линия ветки
- `\` и `/` — слияние

## 📋 Шаг 7: Отправка слияния на GitHub

```bash
# После слияния отправить на GitHub
git push origin main
```

## 📋 Шаг 8: Удаление слитой ветки

После успешного слияния можно удалить ветку:

```bash
# Удалить локальную ветку
git branch -d feature-new-analysis

# Удалить удаленную ветку на GitHub
git push origin --delete feature-new-analysis
```

## 📋 Шаг 9: Разрешение конфликтов при слиянии (базово)

Если Git не может автоматически объединить изменения:

```bash
git merge feature-branch
```

**Результат:**
```
Auto-merging main.py
CONFLICT (content): Merge conflict in main.py
Automatic merge failed; fix conflicts and then commit the result.
```

**Что делать:**
1. Откройте файл с конфликтом
2. Найдите маркеры:
```
<<<<<<< HEAD
Код из main
=======
Код из feature-branch
>>>>>>> feature-branch
```
3. Выберите нужный код или объедините оба
4. Удалите маркеры
5. Сохраните файл
6. Завершите слияние:
```bash
git add .
git commit -m "Разрешен конфликт при слиянии"
```

**Подробнее о конфликтах — в Модуле 5.**

## 📋 Шаг 10: Практический пример

```bash
# 1. Создать ветку для новой функции
git checkout -b feature-calculator

# 2. Добавить файл
echo "def add(a, b): return a + b" > calculator.py
git add calculator.py
git commit -m "Добавлен калькулятор"

# 3. Вернуться на main
git checkout main

# 4. Слить ветку
git merge feature-calculator

# 5. Проверить результат
ls  # должен быть calculator.py
git log --oneline --graph

# 6. Отправить на GitHub
git push origin main

# 7. Удалить ветку (опционально)
git branch -d feature-calculator
```

## ✅ Проверка знаний

1. Создайте ветку `feature-test-merge`
2. Добавьте файл и сделайте коммит
3. Вернитесь на `main`
4. Выполните слияние
5. Проверьте историю коммитов

## 🎓 Что вы узнали

- ✅ Как выполнять слияние веток
- ✅ Типы слияния (fast-forward и merge commit)
- ✅ Как проверять результат слияния
- ✅ Как удалять слитые ветки
- ✅ Базовую работу с конфликтами

## ➡️ Следующий шаг

Переходите к **Уроку 3.3: Работа с несколькими ветками**

---

**💡 Совет:** Всегда проверяйте, что вы на правильной ветке перед слиянием. Обычно сливают в `main`!
