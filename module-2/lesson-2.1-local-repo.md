# Урок 2.1: Создание локального репозитория

## 🎯 Цель урока
Научиться создавать локальный Git-репозиторий для вашего проекта и понимать структуру репозитория.

## 📚 Термины урока

**Local Repository** (EN) → **Локальный репозиторий** (RU) → Репозиторий на вашем компьютере. Пример: папка проекта с инициализированным Git.

**Initialize** (EN) → **Инициализировать** (RU) → Создать новый Git-репозиторий в папке. Команда: `git init`.

## 📋 Шаг 1: Подготовка проекта

Предположим, у вас есть проект (Python-скрипт, Jupyter-ноутбук или веб-приложение), который вы хотите поместить под контроль версий.

**Пример структуры проекта:**
```
my-data-analysis/
├── main.py
├── data.csv
├── requirements.txt
└── README.md
```

## 📋 Шаг 2: Инициализация репозитория

### Способ 1: Через терминал

```bash
# Перейти в папку проекта
cd my-data-analysis

# Инициализировать Git-репозиторий
git init
```

**Результат:**
```
Initialized empty Git repository in /path/to/my-data-analysis/.git/
```

### Способ 2: Через Cursor

1. Откройте папку проекта в Cursor (`File → Open Folder`)
2. Откройте Source Control (`Ctrl + Shift + G`)
3. Нажмите кнопку **"Initialize Repository"**
4. Выберите папку проекта

## 📋 Шаг 3: Что происходит при инициализации

После `git init` создается скрытая папка `.git/`:

```
my-data-analysis/
├── .git/              ← скрытая папка Git
│   ├── config         ← настройки репозитория
│   ├── HEAD           ← указатель на текущую ветку
│   └── objects/       ← хранилище коммитов
├── main.py
└── ...
```

**Важно:** Не удаляйте папку `.git/` — она содержит всю историю проекта!

## 📋 Шаг 4: Проверка статуса репозитория

```bash
git status
```

**Результат для нового репозитория:**
```
On branch main

No commits yet

Untracked files:
  main.py
  data.csv
  requirements.txt
  README.md
```

**Что это значит:**
- `On branch main` — вы на главной ветке
- `No commits yet` — еще нет коммитов
- `Untracked files` — файлы не отслеживаются Git (новые)

## 📋 Шаг 5: Настройка .gitignore (важно!)

**Gitignore** (EN) → **Игнорирование файлов** (RU) → Файл, который указывает Git, какие файлы не нужно отслеживать.

**Зачем это нужно:**
- Не коммитить личные данные
- Не коммитить временные файлы
- Не коммитить большие файлы данных

### Создание .gitignore для Python-проекта:

```bash
# Создать файл .gitignore
touch .gitignore
```

**Содержимое .gitignore для Python:**
```
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/

# Jupyter Notebook
.ipynb_checkpoints
*.ipynb_checkpoints

# Данные (большие файлы)
*.csv
*.xlsx
*.parquet
data/
datasets/

# IDE
.vscode/
.idea/
*.swp

# Системные файлы
.DS_Store
Thumbs.db
```

**Для проекта аналитики данных добавьте:**
```
# Данные
*.csv
*.xlsx
*.parquet
*.h5
data/
raw_data/
processed_data/

# Результаты
outputs/
results/
*.png
*.jpg
*.pdf
```

## 📋 Шаг 6: Первый коммит (быстрый обзор)

```bash
# Добавить все файлы (кроме игнорируемых)
git add .

# Проверить, что добавлено
git status

# Создать первый коммит
git commit -m "Initial commit: добавлен базовый проект"
```

## 📋 Шаг 7: Просмотр истории

```bash
git log
```

**Результат:**
```
commit abc123def456... (HEAD -> main)
Author: Ваше Имя <your@email.com>
Date:   Mon Jan 15 10:30:00 2024

    Initial commit: добавлен базовый проект
```

## ✅ Проверка знаний

1. Создайте новую папку `test-project`
2. Инициализируйте Git-репозиторий
3. Создайте файл `test.py` с кодом: `print("Hello")`
4. Создайте `.gitignore` для Python
5. Сделайте первый коммит

## 🎓 Что вы узнали

- ✅ Как создать локальный репозиторий
- ✅ Что такое `.git/` папка
- ✅ Как проверить статус репозитория
- ✅ Как настроить `.gitignore` для разных типов проектов

## ➡️ Следующий шаг

Переходите к **Уроку 2.2: Первый коммит (commit)**

---

**💡 Совет:** Всегда создавайте `.gitignore` перед первым коммитом, чтобы не закоммитить лишние файлы!
