# Практика 2: Создание первого проекта на GitHub

## 🎯 Цель практики
Создать реальный проект, разместить его на GitHub и научиться синхронизировать изменения.

## 📋 Задание 1: Подготовка проекта

Создайте проект для анализа данных:

```bash
# Создать папку проекта
mkdir my-first-github-project
cd my-first-github-project

# Инициализировать Git
git init
```

## 📋 Задание 2: Создание файлов проекта

Создайте следующие файлы:

### 1. `README.md`
```markdown
# Мой первый проект на GitHub

Проект для анализа данных.

## Установка

```bash
pip install -r requirements.txt
```

## Использование

```bash
python main.py
```
```

### 2. `main.py`
```python
"""
Простой скрипт для анализа данных
"""
import pandas as pd

def analyze_data():
    """Функция для анализа данных"""
    print("Анализ данных...")
    # Здесь будет ваш код анализа
    
if __name__ == "__main__":
    analyze_data()
```

### 3. `requirements.txt`
```
pandas>=1.5.0
numpy>=1.23.0
```

### 4. `.gitignore`
```
# Python
__pycache__/
*.py[cod]
*$py.class
venv/
env/

# Данные
*.csv
*.xlsx
data/

# IDE
.vscode/
.idea/
```

## 📋 Задание 3: Первый коммит

```bash
# Проверить статус
git status

# Добавить все файлы
git add .

# Создать первый коммит
git commit -m "Initial commit: добавлен базовый проект для анализа данных"

# Проверить историю
git log --oneline
```

## 📋 Задание 4: Создание репозитория на GitHub

1. Откройте https://github.com
2. Создайте новый репозиторий:
   - **Name:** `my-first-github-project`
   - **Visibility:** Public
   - **НЕ** добавляйте README, .gitignore или license
3. Скопируйте URL репозитория

## 📋 Задание 5: Связывание с GitHub

```bash
# Добавить remote (замените URL на свой)
git remote add origin https://github.com/username/my-first-github-project.git

# Проверить remote
git remote -v
```

## 📋 Задание 6: Отправка на GitHub

```bash
# Отправить код
git push -u origin main
```

**Если потребуется авторизация:**
- Используйте Personal Access Token (см. Урок 2.3)

## 📋 Задание 7: Проверка на GitHub

1. Откройте ваш репозиторий на GitHub
2. Убедитесь, что все файлы видны
3. Проверьте историю коммитов (вкладка "Commits")

## 📋 Задание 8: Внесение изменений

### Локально:

1. Откройте `main.py` и добавьте функцию:
```python
def load_data(filename):
    """Загрузка данных из файла"""
    try:
        df = pd.read_csv(filename)
        return df
    except FileNotFoundError:
        print(f"Файл {filename} не найден")
        return None
```

2. Обновите `main.py`:
```python
if __name__ == "__main__":
    data = load_data("data.csv")
    if data is not None:
        analyze_data()
```

3. Создайте коммит:
```bash
git add main.py
git commit -m "Добавлена функция загрузки данных"
git push
```

### Проверка:

1. Обновите страницу репозитория на GitHub
2. Убедитесь, что изменения видны
3. Проверьте историю коммитов

## 📋 Задание 9: Работа через интерфейс Cursor

1. Создайте файл `utils.py`:
```python
def calculate_mean(numbers):
    """Вычисление среднего значения"""
    return sum(numbers) / len(numbers) if numbers else 0
```

2. Через Source Control в Cursor:
   - Добавьте файл в staging (`+`)
   - Создайте коммит: "Добавлена утилита для расчетов"
   - Отправьте на GitHub (через `...` → Push)

## 📋 Задание 10: Обновление README

1. Обновите `README.md`:
```markdown
# Мой первый проект на GitHub

Проект для анализа данных.

## Функции

- `analyze_data()` - анализ данных
- `load_data(filename)` - загрузка данных из CSV
- `calculate_mean(numbers)` - вычисление среднего значения

## Установка

```bash
pip install -r requirements.txt
```

## Использование

```bash
python main.py
```
```

2. Создайте коммит и отправьте:
```bash
git add README.md
git commit -m "Обновлен README с описанием функций"
git push
```

## ✅ Критерии успешного выполнения

- ✅ Создан локальный репозиторий
- ✅ Создан репозиторий на GitHub
- ✅ Код отправлен на GitHub
- ✅ Внесены изменения и синхронизированы
- ✅ Работа через интерфейс Cursor освоена

## 🎓 Что вы закрепили

- ✅ Создание репозитория
- ✅ Работу с коммитами
- ✅ Синхронизацию с GitHub
- ✅ Работу через Cursor

## ➡️ Следующий шаг

Если все задания выполнены, переходите к **Модулю 3: Ветвление и слияние**

---

**💡 Совет:** Сохраните этот проект — он будет примером для дальнейшего обучения!
