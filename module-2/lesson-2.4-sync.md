# Урок 2.4: Синхронизация локального и удаленного репозитория

## 🎯 Цель урока
Научиться синхронизировать изменения между локальным компьютером и GitHub: отправлять изменения (push) и получать их (pull).

## 📚 Термины урока

**Push** (EN) → **Отправка** (RU) → Отправка ваших локальных коммитов на GitHub. Команда: `git push`.

**Pull** (EN) → **Получение** (RU) → Получение изменений с GitHub на ваш компьютер. Команда: `git pull`.

**Fetch** (EN) → **Загрузка** (RU) → Загрузка информации об изменениях с GitHub без слияния. Команда: `git fetch`.

## 📋 Шаг 1: Отправка изменений на GitHub (Push)

### Процесс работы:

1. Вы делаете изменения в файлах локально
2. Создаете коммит
3. Отправляете на GitHub

### Команды:

```bash
# 1. Проверить статус
git status

# 2. Добавить изменения в staging
git add .

# 3. Создать коммит
git commit -m "Описание изменений"

# 4. Отправить на GitHub
git push origin main
```

**После первого push с `-u`:**
```bash
# Можно просто использовать
git push
```

### Через интерфейс Cursor:

1. Создайте коммит через Source Control
2. Нажмите `...` (More Actions)
3. Выберите **"Push"** или **"Push to..."**

## 📋 Шаг 2: Получение изменений с GitHub (Pull)

### Когда это нужно:

- Вы работали на другом компьютере
- Кто-то внес изменения в репозиторий
- Вы хотите обновить локальную версию

### Команды:

```bash
# Получить изменения и автоматически слить
git pull origin main

# Или просто (если установлена связь)
git pull
```

**Что происходит:**
1. Git загружает изменения с GitHub
2. Автоматически сливает их с вашими локальными изменениями
3. Если конфликтов нет — всё готово

### Через интерфейс Cursor:

1. Нажмите `...` в Source Control
2. Выберите **"Pull"** или **"Pull from..."**

## 📋 Шаг 3: Загрузка информации без слияния (Fetch)

**Fetch** — безопасный способ посмотреть, что изменилось на GitHub, без автоматического слияния.

```bash
# Загрузить информацию об изменениях
git fetch origin

# Посмотреть, что изменилось
git log HEAD..origin/main

# Если всё хорошо, сделать pull
git pull
```

**Зачем это нужно:**
- Посмотреть изменения перед слиянием
- Убедиться, что нет конфликтов
- Решить, когда делать pull

## 📋 Шаг 4: Проверка состояния синхронизации

```bash
# Показать информацию о ветках
git branch -vv
```

**Результат:**
```
* main abc123 [origin/main: ahead 2, behind 1] Последний коммит
```

**Что это значит:**
- `ahead 2` — у вас 2 коммита, которых нет на GitHub (нужен push)
- `behind 1` — на GitHub 1 коммит, которого нет у вас (нужен pull)

## 📋 Шаг 5: Типичный workflow работы

### Сценарий 1: Работа на одном компьютере

```bash
# 1. Внести изменения в файлы
# 2. Добавить в staging
git add .

# 3. Создать коммит
git commit -m "Новая функция"

# 4. Отправить на GitHub
git push
```

### Сценарий 2: Работа на нескольких компьютерах

**На компьютере 1:**
```bash
git add .
git commit -m "Изменения"
git push
```

**На компьютере 2:**
```bash
# Сначала получить изменения
git pull

# Затем работать
# ... вносите изменения ...
git add .
git commit -m "Еще изменения"
git push
```

### Сценарий 3: Начало работы дня

```bash
# Всегда начинайте с pull
git pull

# Затем работайте
# ... вносите изменения ...
git add .
git commit -m "Изменения"
git push
```

## 📋 Шаг 6: Разрешение конфликтов (кратко)

Если при `git pull` возник конфликт:

```bash
# Git покажет конфликтующие файлы
# Откройте файл и найдите маркеры:
<<<<<<< HEAD
Ваш код
=======
Код с GitHub
>>>>>>> origin/main

# Выберите нужный код или объедините
# Затем:
git add .
git commit -m "Разрешен конфликт"
git push
```

**Подробнее о конфликтах — в Модуле 5.**

## 📋 Шаг 7: Просмотр удаленных репозиториев

```bash
# Показать все remote
git remote -v

# Показать информацию о remote
git remote show origin
```

## ✅ Проверка знаний

1. Создайте новый файл локально
2. Сделайте коммит и отправьте на GitHub
3. Проверьте на GitHub, что файл появился
4. Внесите изменение в файл на GitHub (через веб-интерфейс)
5. Выполните `git pull` и проверьте, что изменение появилось локально

## 🎓 Что вы узнали

- ✅ Как отправлять изменения на GitHub (push)
- ✅ Как получать изменения с GitHub (pull)
- ✅ Как проверять состояние синхронизации
- ✅ Типичные сценарии работы

## ➡️ Следующий шаг

Переходите к **Практике 2: Создание первого проекта на GitHub**

---

**💡 Совет:** Всегда делайте `git pull` перед началом работы, чтобы быть в курсе последних изменений!
