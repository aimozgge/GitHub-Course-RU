# Урок 2.2: Первый коммит (commit)

## 🎯 Цель урока
Научиться создавать коммиты правильно: понимать staging area, писать хорошие сообщения коммитов.

## 📚 Термины урока

**Commit** (EN) → **Коммит** (RU) → Сохранение изменений с описанием. Это "снимок" проекта в определенный момент.

**Staging Area** (EN) → **Область подготовки** (RU) → Промежуточная область, куда добавляются файлы перед коммитом. Команда: `git add`.

**Commit Message** (EN) → **Сообщение коммита** (RU) → Описание того, что изменилось в этом коммите.

## 📋 Шаг 1: Понимание workflow Git

Процесс работы с Git состоит из трех этапов:

```
Рабочая директория  →  Staging Area  →  Repository
   (изменения)         (git add)      (git commit)
```

**Пример:**
1. Вы редактируете `main.py` (рабочая директория)
2. `git add main.py` (добавляете в staging)
3. `git commit -m "..."` (сохраняете в репозиторий)

## 📋 Шаг 2: Проверка изменений

```bash
git status
```

**Возможные статусы файлов:**
- **Untracked** — новый файл, Git его не отслеживает
- **Modified** — файл изменен, но не в staging
- **Staged** — файл добавлен в staging, готов к коммиту
- **Unmodified** — файл не изменен

## 📋 Шаг 3: Добавление файлов в staging

### Добавить один файл:
```bash
git add main.py
```

### Добавить все измененные файлы:
```bash
git add .
```

### Добавить все файлы определенного типа:
```bash
git add *.py
```

### Добавить все файлы в папке:
```bash
git add src/
```

## 📋 Шаг 4: Проверка того, что добавлено

```bash
git status
```

**После `git add`:**
```
On branch main
Changes to be committed:
  (use "git reset HEAD <file>..." to unstage)
  
        new file:   main.py
        modified:   README.md
```

**Через интерфейс Cursor:**
- Файлы в staging отображаются в разделе "Staged Changes"
- Рядом с файлом стоит `+` (добавлен)

## 📋 Шаг 5: Удаление из staging (если нужно)

Если вы случайно добавили файл в staging:

```bash
# Убрать один файл
git reset HEAD main.py

# Убрать все файлы
git reset HEAD
```

**В Cursor:** Нажмите `-` рядом с файлом в "Staged Changes"

## 📋 Шаг 6: Создание коммита

### Способ 1: Через терминал

```bash
git commit -m "Описание изменений"
```

**Примеры хороших сообщений:**
```bash
git commit -m "Добавлена функция анализа данных"
git commit -m "Исправлена ошибка в расчете статистики"
git commit -m "Обновлен README с инструкциями"
```

### Способ 2: Через Cursor

1. Откройте Source Control (`Ctrl + Shift + G`)
2. Убедитесь, что файлы в staging
3. Введите сообщение коммита в поле сверху
4. Нажмите `✓` (Commit) или `Ctrl + Enter`

## 📋 Шаг 7: Правила написания сообщений коммитов

### ✅ Хорошие сообщения:

```
Добавлена функция загрузки данных из CSV
Исправлена ошибка в расчете среднего значения
Обновлен requirements.txt с новыми зависимостями
Добавлена документация для функции analyze()
```

### ❌ Плохие сообщения:

```
fix
обновление
изменения
asdf
```

**Правила:**
- Начинайте с глагола в повелительном наклонении (Добавлена, Исправлена, Обновлен)
- Будьте конкретными (что именно изменилось)
- Используйте русский язык (если команда русскоязычная)
- Ограничьте длину первой строки до 50-72 символов

## 📋 Шаг 8: Коммит с подробным описанием

Для важных изменений можно добавить подробное описание:

```bash
git commit
```

Откроется редактор, где можно написать:
```
Краткое описание (первая строка)

Подробное описание изменений:
- Что было изменено
- Почему это было изменено
- Какие файлы затронуты
```

**Или через одну команду:**
```bash
git commit -m "Краткое описание" -m "Подробное описание"
```

## 📋 Шаг 9: Просмотр созданного коммита

```bash
git log
```

**Результат:**
```
commit abc123def456789... (HEAD -> main)
Author: Ваше Имя <your@email.com>
Date:   Mon Jan 15 10:30:00 2024

    Добавлена функция анализа данных
```

**Краткий формат:**
```bash
git log --oneline
```

**Результат:**
```
abc123 Добавлена функция анализа данных
def456 Исправлена ошибка в расчете
```

## 📋 Шаг 10: Просмотр изменений в коммите

```bash
# Показать изменения последнего коммита
git show

# Показать изменения конкретного коммита
git show abc123

# Показать изменения в файле
git diff main.py
```

## ✅ Проверка знаний

1. Создайте файл `utils.py` с функцией
2. Добавьте файл в staging
3. Создайте коммит с хорошим сообщением
4. Проверьте историю коммитов

## 🎓 Что вы узнали

- ✅ Как работает staging area
- ✅ Как добавлять файлы в staging
- ✅ Как создавать коммиты
- ✅ Как писать хорошие сообщения коммитов
- ✅ Как просматривать историю

## ➡️ Следующий шаг

Переходите к **Уроку 2.3: Создание репозитория на GitHub**

---

**💡 Совет:** Делайте коммиты часто и с понятными сообщениями. Это поможет отслеживать историю изменений проекта!
