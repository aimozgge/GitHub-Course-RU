# Практика 1: Проверка установки и настройки

## 🎯 Цель практики
Закрепить знания из Модуля 1 и убедиться, что всё настроено правильно.

## 📋 Задание 1: Проверка Git

Выполните в терминале Cursor:

```bash
# 1. Проверить версию Git
git --version

# 2. Проверить настройки
git config --global user.name
git config --global user.email

# 3. Показать все настройки
git config --list --global
```

**Ожидаемый результат:**
- Видна версия Git (например, `git version 2.40.0`)
- Видны ваше имя и email
- Список всех настроек

---

## 📋 Задание 2: Создание тестового репозитория

```bash
# 1. Создать папку для практики
mkdir git-practice-1
cd git-practice-1

# 2. Инициализировать Git-репозиторий
git init

# 3. Проверить статус
git status
```

**Ожидаемый результат:**
- Сообщение: `Initialized empty Git repository`
- Статус показывает, что вы на ветке `main` (или `master`)

---

## 📋 Задание 3: Работа с файлами

```bash
# 1. Создать файл README.md
echo "# Практика Git" > README.md

# 2. Проверить статус
git status

# 3. Добавить файл в staging
git add README.md

# 4. Проверить статус снова
git status

# 5. Создать первый коммит
git commit -m "Добавлен README.md"
```

**Ожидаемый результат:**
- После `git status` виден `README.md` как untracked
- После `git add` файл в staging area
- После `git commit` коммит создан успешно

---

## 📋 Задание 4: Работа через интерфейс Cursor

1. Откройте папку `git-practice-1` в Cursor
2. Создайте новый файл `test.py` с содержимым:
```python
print("Hello, Git!")
```

3. Откройте Source Control (`Ctrl + Shift + G`)
4. Добавьте файл в staging (кнопка `+`)
5. Создайте коммит с сообщением "Добавлен test.py"

**Ожидаемый результат:**
- Файл виден в панели Source Control
- Коммит создан через интерфейс Cursor

---

## 📋 Задание 5: Просмотр истории

```bash
# Показать историю коммитов
git log
```

**Ожидаемый результат:**
- Видны два коммита:
  1. "Добавлен README.md"
  2. "Добавлен test.py"

---

## 📋 Задание 6: Проверка терминологии

Ответьте на вопросы (можно письменно):

1. Что такое репозиторий в контексте Git?
2. В чем разница между `git add` и `git commit`?
3. Что означает сообщение "On branch main" в `git status`?
4. Зачем нужен staging area?

---

## ✅ Критерии успешного выполнения

- ✅ Git установлен и настроен
- ✅ Создан тестовый репозиторий
- ✅ Созданы коммиты через терминал
- ✅ Создан коммит через интерфейс Cursor
- ✅ Понята разница между основными командами

---

## 🐛 Возможные проблемы и решения

### Проблема: "git: command not found"
**Решение:** Git не установлен. Вернитесь к Уроку 1.1.

### Проблема: "Please tell me who you are"
**Решение:** Не настроены имя и email. Выполните:
```bash
git config --global user.name "Ваше Имя"
git config --global user.email "your@email.com"
```

### Проблема: "fatal: not a git repository"
**Решение:** Вы не в папке с Git-репозиторием. Выполните `git init` или перейдите в нужную папку.

---

## 🎓 Что вы закрепили

- ✅ Настройку Git
- ✅ Создание репозитория
- ✅ Работу с коммитами
- ✅ Использование интерфейса Cursor

## ➡️ Следующий шаг

Если все задания выполнены успешно, переходите к **Модулю 2: Работа с репозиториями**

---

**💡 Совет:** Сохраните папку `git-practice-1` — она пригодится для следующих практик!
