# Урок 1.3: Первое знакомство с терминологией Git

## 🎯 Цель урока
Понять основные термины Git на практических примерах, чтобы свободно ориентироваться в дальнейших уроках.

## 📚 Основные термины Git

### 1. Repository (Репозиторий)

**Repository** (EN) → **Репозиторий** (RU) → Папка с проектом, которая находится под контролем Git. Хранит всю историю изменений.

**Пример:**
```
my-project/          ← это репозиторий
├── .git/           ← скрытая папка Git (хранит историю)
├── main.py
└── README.md
```

**Аналогия:** Репозиторий — это как папка с проектом, но с "машиной времени" для всех файлов.

---

### 2. Commit (Коммит)

**Commit** (EN) → **Коммит** (RU) → Сохранение текущего состояния файлов с описанием изменений. Это как "снимок" проекта в определенный момент.

**Пример:**
```bash
git commit -m "Добавлена функция анализа данных"
```

**Аналогия:** Коммит — это как сохранение в игре, к которому можно вернуться.

**Структура коммита:**
- Уникальный ID (хеш)
- Автор и дата
- Сообщение (что изменилось)
- Список измененных файлов

---

### 3. Branch (Ветка)

**Branch** (EN) → **Ветка** (RU) → Отдельная линия разработки. Позволяет работать над разными функциями параллельно, не мешая основной версии.

**Пример:**
```
main (основная ветка)
├── commit 1
├── commit 2
└── commit 3
    │
    ├── feature-analysis (новая ветка)
    │   ├── commit 4
    │   └── commit 5
```

**Аналогия:** Ветка — это как отдельная копия проекта для экспериментов.

**Основные ветки:**
- `main` или `master` — основная ветка проекта
- `feature-*` — ветки для новых функций
- `bugfix-*` — ветки для исправления ошибок

---

### 4. Merge (Слияние)

**Merge** (EN) → **Слияние** (RU) → Объединение изменений из одной ветки в другую.

**Пример:**
```
main:     A---B---C
                \
feature:         D---E
                  \
main (после merge): A---B---C---F (F = merge D+E)
```

**Аналогия:** Слияние — это как объединение двух версий документа в одну.

---

### 5. Clone (Клонирование)

**Clone** (EN) → **Клонирование** (RU) → Копирование репозитория с GitHub на ваш компьютер.

**Пример:**
```bash
git clone https://github.com/username/project-name.git
```

**Аналогия:** Клонирование — это как скачать весь проект с историей изменений.

---

### 6. Fork (Форк)

**Fork** (EN) → **Форк** (RU) → Создание копии чужого репозитория в вашем аккаунте GitHub. Позволяет вносить изменения без влияния на оригинал.

**Пример:**
```
Оригинальный репозиторий: github.com/author/project
Ваш форк:                  github.com/yourname/project
```

**Аналогия:** Форк — это как сделать копию чужой книги для своих заметок.

---

### 7. Pull Request (PR)

**Pull Request** (EN) → **Запрос на слияние** (RU) → Запрос на внесение ваших изменений в чужой проект.

**Процесс:**
1. Вы форкаете репозиторий
2. Делаете изменения
3. Создаете Pull Request
4. Владелец проекта решает, принять изменения или нет

**Аналогия:** PR — это как предложение улучшений автору проекта.

---

### 8. Staging Area (Область подготовки)

**Staging Area** (EN) → **Область подготовки** (RU) → Промежуточная область, куда добавляются файлы перед коммитом.

**Процесс:**
```
Рабочая директория → Staging Area → Commit
   (изменения)      (git add)      (git commit)
```

**Команды:**
```bash
git add file.py        # добавить файл в staging
git add .              # добавить все изменения
git commit -m "..."    # создать коммит из staging
```

**Аналогия:** Staging — это как корзина покупок перед оплатой.

---

### 9. Remote (Удаленный репозиторий)

**Remote** (EN) → **Удаленный репозиторий** (RU) → Репозиторий на GitHub (или другом сервере), с которым синхронизируется локальный репозиторий.

**Пример:**
```bash
git remote add origin https://github.com/username/repo.git
git push origin main    # отправить изменения на GitHub
git pull origin main    # получить изменения с GitHub
```

**Аналогия:** Remote — это как облачное хранилище для вашего проекта.

---

### 10. Push и Pull

**Push** (EN) → **Отправка** (RU) → Отправка ваших локальных коммитов на GitHub.

**Pull** (EN) → **Получение** (RU) → Получение изменений с GitHub на ваш компьютер.

**Пример:**
```bash
git push origin main    # отправить изменения
git pull origin main    # получить изменения
```

**Аналогия:** 
- Push — загрузить фото в облако
- Pull — скачать фото из облака

---

## 🎯 Визуальная схема работы Git

```
Локальный компьютер          GitHub
┌─────────────┐            ┌─────────────┐
│  Working    │            │   Remote    │
│  Directory  │            │ Repository  │
│             │            │             │
│  file.py    │   push     │  file.py    │
│  (изменен)  │ ────────>  │  (обновлен) │
│             │            │             │
│  git add    │   pull     │             │
│  git commit │ <────────  │             │
└─────────────┘            └─────────────┘
```

## ✅ Проверка знаний

Ответьте на вопросы:

1. Что такое коммит и зачем он нужен?
2. В чем разница между `git push` и `git pull`?
3. Что такое ветка и когда её используют?
4. Зачем нужен staging area?

## 🎓 Что вы узнали

- ✅ Поняли основные термины Git
- ✅ Узнали аналогии для лучшего понимания
- ✅ Познакомились с процессом работы Git

## ➡️ Следующий шаг

Переходите к **Практике 1: Проверка установки и настройки**

---

**💡 Совет:** Не пытайтесь запомнить все сразу. Термины станут понятнее, когда начнете использовать их на практике в следующих модулях.
