# Урок 1.2: Настройка Git в Cursor

## 🎯 Цель урока
Настроить интеграцию Git в Cursor IDE для удобной работы с версионированием прямо из редактора.

## 📚 Термины урока

**IDE Integration** (EN) → **Интеграция в среду разработки** (RU) → Возможность использовать Git через интерфейс редактора, а не только через терминал. Пример: кнопки для коммита в панели Cursor.

**Source Control** (EN) → **Контроль версий** (RU) → Панель в Cursor, где отображаются все изменения в файлах проекта.

## 📋 Шаг 1: Открытие панели Source Control в Cursor

1. Нажмите на иконку **Source Control** в левой панели (иконка с ветвлением)
   - Или используйте горячую клавишу: `Ctrl + Shift + G`
2. Если папка не является Git-репозиторием, вы увидите кнопку **"Initialize Repository"**

## 📋 Шаг 2: Проверка встроенного Git в Cursor

Cursor автоматически определяет Git, если он установлен в системе.

**Проверка:**
1. Откройте терминал в Cursor (`Ctrl + ` `)
2. Выполните: `git --version`
3. Если команда работает, Cursor тоже будет видеть Git

## 📋 Шаг 3: Настройка видимости Git в Cursor

### Включение индикаторов изменений:

1. Откройте настройки: `File → Preferences → Settings` (или `Ctrl + ,`)
2. В поиске введите: `git.enabled`
3. Убедитесь, что стоит галочка ✅

### Настройка отображения изменений:

В настройках найдите и включите:
- `git.decorations.enabled` — показывает индикаторы изменений рядом с файлами
- `scm.diffDecorations` — подсветка изменений в редакторе

## 📋 Шаг 4: Знакомство с панелью Source Control

В панели Source Control вы увидите:

- **Changes** (Изменения) — список измененных файлов
  - `M` — Modified (изменен)
  - `U` — Untracked (новый файл, не отслеживается)
  - `D` — Deleted (удален)

- **Кнопки:**
  - **+** (Stage Changes) — добавить файл в staging area
  - **✓** (Commit) — создать коммит
  - **...** (More Actions) — дополнительные действия

## 📋 Шаг 5: Создание тестового репозитория для проверки

Давайте создадим тестовую папку и инициализируем Git:

```bash
# Создать папку для теста
mkdir test-git-repo
cd test-git-repo

# Инициализировать Git-репозиторий
git init

# Создать тестовый файл
echo "# Тестовый проект" > README.md
```

Теперь вернитесь в Cursor и откройте папку `test-git-repo`:
1. `File → Open Folder`
2. Выберите папку `test-git-repo`

## 📋 Шаг 6: Проверка работы Git в Cursor

1. Откройте панель Source Control (`Ctrl + Shift + G`)
2. Вы должны увидеть файл `README.md` в списке изменений
3. Попробуйте:
   - Нажать `+` рядом с файлом (добавить в staging)
   - Ввести сообщение коммита: "Первый коммит"
   - Нажать `✓` (Commit)

**Поздравляю!** Вы создали первый коммит через интерфейс Cursor! 🎉

## 📋 Шаг 7: Полезные горячие клавиши

- `Ctrl + Shift + G` — открыть/закрыть Source Control
- `Ctrl + K, Ctrl + S` — показать все горячие клавиши Git
- В редакторе: `Ctrl + K, Ctrl + G` — открыть изменения в файле

## ✅ Проверка знаний

Выполните:
1. Создайте новый файл `test.py` в тестовом репозитории
2. Откройте Source Control
3. Добавьте файл в staging (кнопка `+`)
4. Создайте коммит с сообщением "Добавлен test.py"

## 🎓 Что вы узнали

- ✅ Настроили Git в Cursor
- ✅ Познакомились с панелью Source Control
- ✅ Создали первый коммит через интерфейс Cursor
- ✅ Узнали горячие клавиши для работы с Git

## ➡️ Следующий шаг

Переходите к **Уроку 1.3: Первое знакомство с терминологией**

---

**💡 Совет:** Вы можете работать с Git как через терминал, так и через интерфейс Cursor. Выбирайте то, что удобнее. Для начинающих интерфейс Cursor проще.
