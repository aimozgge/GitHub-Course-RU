# Урок 1.1: Установка и настройка Git

## 🎯 Цель урока
Установить Git на ваш компьютер и выполнить базовую настройку.

## 📚 Термины урока

**Git** (EN) → **Система контроля версий** (RU) → Программа, которая отслеживает все изменения в ваших файлах и позволяет вернуться к любой версии проекта.

**Repository** (EN) → **Репозиторий** (RU) → Папка с проектом, которая находится под контролем Git. Пример: папка `my-project/` становится репозиторием после команды `git init`.

## 📋 Шаг 1: Проверка установки Git

Откройте терминал в Cursor (`` Ctrl + ` `` или `View → Terminal`) и выполните:

```bash
git --version
```

**Если Git установлен**, вы увидите что-то вроде: `git version 2.40.0`

**Если Git не установлен**, переходите к Шагу 2.

## 📋 Шаг 2: Установка Git (если не установлен)

### Для Windows:
1. Скачайте Git с официального сайта: https://git-scm.com/download/win
2. Запустите установщик
3. Используйте настройки по умолчанию (просто нажимайте "Next")
4. После установки перезапустите Cursor

### Проверка установки:
```bash
git --version
```

## 📋 Шаг 3: Настройка Git (первоначальная конфигурация)

Выполните эти команды в терминале (замените на свои данные):

```bash
# Указать ваше имя (будет видно в коммитах)
git config --global user.name "Ваше Имя"

# Указать ваш email (желательно тот, что используется на GitHub)
git config --global user.email "your.email@example.com"
```

**Пример:**
```bash
git config --global user.name "Иван Петров"
git config --global user.email "ivan@example.com"
```

## 📋 Шаг 4: Проверка настроек

Проверьте, что настройки применились:

```bash
git config --global user.name
git config --global user.email
```

Должны отобразиться ваши имя и email.

## 📋 Шаг 5: Дополнительные полезные настройки

```bash
# Установить редактор по умолчанию (для Cursor можно оставить по умолчанию)
git config --global core.editor "code --wait"

# Установить имя главной ветки как 'main' (современный стандарт)
git config --global init.defaultBranch main

# Включить цветной вывод (удобнее читать)
git config --global color.ui auto
```

## ✅ Проверка знаний

Выполните команду для просмотра всех настроек:

```bash
git config --list --global
```

Вы должны увидеть все ваши настройки, включая `user.name` и `user.email`.

## 🎓 Что вы узнали

- ✅ Установили Git (если нужно было)
- ✅ Настроили имя и email для коммитов
- ✅ Настроили дополнительные параметры для удобства работы

## ➡️ Следующий шаг

Переходите к **Уроку 1.2: Настройка Git в Cursor**

---

**💡 Совет:** Все настройки Git хранятся в файле `~/.gitconfig`. Вы можете редактировать их вручную, но проще через команды `git config`.
