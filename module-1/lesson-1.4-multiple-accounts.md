# Урок 1.4: Работа с несколькими аккаунтами GitHub

## 🎯 Цель урока
Научиться использовать несколько аккаунтов GitHub на одном компьютере для разных проектов.

## 📚 Термины урока

**Multiple Accounts** (EN) → **Несколько аккаунтов** (RU) → Использование разных GitHub аккаунтов для разных проектов.

**SSH Keys** (EN) → **SSH ключи** (RU) → Пара ключей (публичный и приватный) для безопасной авторизации без пароля.

**Git Config** (EN) → **Конфигурация Git** (RU) → Настройки Git для конкретного проекта или глобально.

## 📋 Шаг 1: Зачем нужны несколько аккаунтов?

**Причины:**
- Личные и рабочие проекты разделены
- Разные проекты для разных целей
- Работа над open-source под разными именами

## 📋 Шаг 2: Стратегии работы с несколькими аккаунтами

### Стратегия 1: Разные конфигурации для разных проектов (рекомендуется)

**Идея:** Глобальная конфигурация для основного аккаунта, локальная — для каждого проекта.

### Стратегия 2: Разные SSH ключи для разных аккаунтов

**Идея:** Каждый аккаунт использует свой SSH ключ.

## 📋 Шаг 3: Настройка глобальной конфигурации (основной аккаунт)

Ваша текущая глобальная конфигурация:
```bash
git config --global user.name "aimozgge"
git config --global user.email "aimozgge@gmail.com"
```

**Это будет использоваться по умолчанию для всех проектов.**

## 📋 Шаг 4: Настройка локальной конфигурации для проекта

Для конкретного проекта можно задать другую конфигурацию:

```bash
# Перейти в папку проекта
cd путь/к/проекту

# Установить локальную конфигурацию (только для этого проекта)
git config user.name "Новое Имя"
git config user.email "новый-email@gmail.com"

# Проверить конфигурацию
git config user.name
git config user.email
```

**Важно:** Локальная конфигурация переопределяет глобальную только для этого проекта!

## 📋 Шаг 5: Проверка конфигурации

```bash
# Показать все настройки (глобальные + локальные)
git config --list

# Показать только локальные настройки проекта
git config --local --list

# Показать только глобальные настройки
git config --global --list
```

## 📋 Шаг 6: Создание нового аккаунта GitHub

1. Откройте https://github.com
2. Нажмите "Sign up"
3. Создайте новый аккаунт с другим email
4. Подтвердите email

**Рекомендация:** Используйте разные email для разных аккаунтов.

## 📋 Шаг 7: Настройка для нового аккаунта (локально для проекта)

```bash
# В папке проекта для нового аккаунта
cd путь/к/проекту-для-нового-аккаунта

# Установить локальную конфигурацию
git config user.name "Имя Нового Аккаунта"
git config user.email "новый-аккаунт@gmail.com"

# Проверить
git config user.name
git config user.email
```

## 📋 Шаг 8: Авторизация на GitHub (Personal Access Token)

Для каждого аккаунта нужен свой токен:

### Для первого аккаунта (aimozgge):

1. Войдите в GitHub под аккаунтом `aimozgge`
2. Settings → Developer settings → Personal access tokens → Tokens (classic)
3. Generate new token (classic)
4. Выберите права: `repo` (полный доступ к репозиториям)
5. Скопируйте токен

### Для второго аккаунта:

1. Войдите в GitHub под новым аккаунтом
2. Повторите шаги выше
3. Создайте новый токен

**Важно:** Каждый аккаунт имеет свой токен!

## 📋 Шаг 9: Использование токенов при push

При `git push` GitHub запросит:
- **Username:** ваш логин GitHub
- **Password:** используйте токен (не пароль!)

**Для разных аккаунтов:**
- Проект 1 (aimozgge): используйте токен от aimozgge
- Проект 2 (новый аккаунт): используйте токен от нового аккаунта

## 📋 Шаг 10: Альтернатива — SSH ключи (продвинутый способ)

### Создание SSH ключей для каждого аккаунта:

```bash
# Для первого аккаунта
ssh-keygen -t ed25519 -C "aimozgge@gmail.com" -f ~/.ssh/id_ed25519_aimozgge

# Для второго аккаунта
ssh-keygen -t ed25519 -C "новый-аккаунт@gmail.com" -f ~/.ssh/id_ed25519_newaccount
```

### Настройка SSH config:

Создайте файл `~/.ssh/config`:

```
# Аккаунт 1 (aimozgge)
Host github-aimozgge
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_aimozgge

# Аккаунт 2 (новый)
Host github-newaccount
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_newaccount
```

### Использование:

```bash
# Для проекта с первым аккаунтом
git remote set-url origin git@github-aimozgge:username/repo.git

# Для проекта со вторым аккаунтом
git remote set-url origin git@github-newaccount:username/repo.git
```

**Это более сложный способ, но удобнее для постоянной работы.**

## 📋 Шаг 11: Практический пример

### Сценарий: Два проекта, два аккаунта

**Проект 1 (личный, аккаунт aimozgge):**
```bash
cd C:\Users\musae\Projects\personal-project

# Глобальная конфигурация уже установлена (aimozgge)
# Можно использовать как есть, или явно установить:
git config user.name "aimozgge"
git config user.email "aimozgge@gmail.com"

# При push используйте токен от aimozgge
```

**Проект 2 (рабочий, новый аккаунт):**
```bash
cd C:\Users\musae\Projects\work-project

# Установить локальную конфигурацию
git config user.name "Work Account"
git config user.email "work-account@gmail.com"

# При push используйте токен от нового аккаунта
```

## 📋 Шаг 12: Проверка конфигурации в проекте

```bash
# В любом проекте проверить, какая конфигурация используется
git config user.name
git config user.email

# Показать все настройки (локальные + глобальные)
git config --list --show-origin
```

**Последняя настройка в списке имеет приоритет!**

## 📋 Шаг 13: Быстрое переключение (скрипт)

Можно создать скрипт для быстрого переключения:

**Windows PowerShell (`switch-git-account.ps1`):**
```powershell
param(
    [Parameter(Mandatory=$true)]
    [ValidateSet("personal", "work")]
    [string]$Account
)

if ($Account -eq "personal") {
    git config user.name "aimozgge"
    git config user.email "aimozgge@gmail.com"
    Write-Host "Переключено на личный аккаунт: aimozgge"
} elseif ($Account -eq "work") {
    git config user.name "Work Account"
    git config user.email "work-account@gmail.com"
    Write-Host "Переключено на рабочий аккаунт"
}

Write-Host "Текущая конфигурация:"
git config user.name
git config user.email
```

**Использование:**
```powershell
.\switch-git-account.ps1 -Account personal
.\switch-git-account.ps1 -Account work
```

## ✅ Проверка знаний

1. Проверьте текущую глобальную конфигурацию
2. Создайте тестовый проект
3. Установите для него локальную конфигурацию с другим email
4. Проверьте, что локальная конфигурация переопределяет глобальную

## 🎓 Что вы узнали

- ✅ Как использовать несколько аккаунтов GitHub
- ✅ Разницу между глобальной и локальной конфигурацией
- ✅ Как настроить разные аккаунты для разных проектов
- ✅ Как использовать Personal Access Tokens
- ✅ Альтернативный способ через SSH ключи

## ➡️ Следующий шаг

Продолжайте обучение с **Уроком 1.3** или переходите к **Модулю 2**

---

**💡 Совет:** Используйте локальную конфигурацию для проектов с другим аккаунтом. Это проще, чем менять глобальную настройку каждый раз!

## 📝 Краткая шпаргалка

```bash
# Глобальная конфигурация (для всех проектов)
git config --global user.name "aimozgge"
git config --global user.email "aimozgge@gmail.com"

# Локальная конфигурация (только для текущего проекта)
git config user.name "Другое Имя"
git config user.email "другой-email@gmail.com"

# Проверить конфигурацию
git config user.name
git config user.email
```
