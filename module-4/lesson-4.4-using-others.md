# Урок 4.4: Использование чужих проектов в своих задачах

## 🎯 Цель урока
Научиться использовать код из чужих репозиториев в своих проектах: копирование функций, использование как библиотеки, адаптация под свои нужды.

## 📚 Термины урока

**Code Reuse** (EN) → **Повторное использование кода** (RU) → Использование готового кода из других проектов в своем.

**Dependency** (EN) → **Зависимость** (RU) → Внешняя библиотека или пакет, который использует ваш проект.

**License Compliance** (EN) → **Соблюдение лицензии** (RU) → Следование правилам использования кода, указанным в лицензии проекта.

## 📋 Шаг 1: Способы использования чужого кода

### Способ 1: Использование как библиотеки (установка через pip)

**Когда:** Проект опубликован в PyPI (Python Package Index)

```bash
# Установить пакет
pip install package-name

# Использовать в коде
import package_name
```

### Способ 2: Клонирование и использование локально

**Когда:** Проект не в PyPI, но есть на GitHub

```bash
# Клонировать
git clone https://github.com/username/package.git

# Установить в режиме разработки
cd package
pip install -e .
```

### Способ 3: Копирование нужных функций

**Когда:** Нужна только часть кода, не весь проект

## 📋 Шаг 2: Проверка лицензии

**ВАЖНО:** Всегда проверяйте лицензию перед использованием!

```bash
# Проверить лицензию
cat LICENSE
# Или
cat LICENSE.txt
```

**Популярные лицензии:**

- **MIT** — можно использовать свободно, нужно указать автора
- **Apache 2.0** — можно использовать, нужно указать изменения
- **GPL** — можно использовать, но ваш код тоже должен быть открытым
- **Proprietary** — нельзя использовать без разрешения

**Что делать:**
- Если лицензия разрешает — можно использовать
- Всегда указывайте источник (автора)
- Сохраняйте информацию о лицензии

## 📋 Шаг 3: Использование как библиотеки (через pip)

### Если проект в PyPI:

```bash
# Установить
pip install package-name

# Использовать в коде
import package_name

# Пример
pip install pandas
import pandas as pd
```

### Если проект на GitHub (не в PyPI):

```bash
# Установить напрямую с GitHub
pip install git+https://github.com/username/package.git

# Или конкретную версию
pip install git+https://github.com/username/package.git@v1.0.0
```

## 📋 Шаг 4: Клонирование и использование локально

```bash
# 1. Клонировать репозиторий
git clone https://github.com/username/useful-tool.git
cd useful-tool

# 2. Установить в режиме разработки
pip install -e .

# 3. Использовать в своем проекте
# В вашем проекте:
import useful_tool
```

**Режим разработки (`-e`):**
- Изменения в коде сразу доступны
- Не нужно переустанавливать после изменений

## 📋 Шаг 5: Копирование нужных функций

### Когда это уместно:

- Нужна только одна функция
- Проект слишком большой
- Хотите адаптировать под свои нужды

### Процесс:

1. **Найти нужную функцию:**
```bash
# В клонированном репозитории
grep -r "def function_name" .
```

2. **Изучить зависимости функции:**
   - Какие импорты использует
   - Какие другие функции вызывает
   - Какие зависимости нужны

3. **Скопировать с указанием источника:**
```python
# В вашем проекте
"""
Функция загрузки данных
Источник: https://github.com/username/project/blob/main/utils.py
Лицензия: MIT
Автор: Original Author
"""

def load_data(filename):
    """Загрузка данных из файла.
    
    Адаптировано из: https://github.com/username/project
    """
    # ... скопированный код ...
    pass
```

## 📋 Шаг 6: Адаптация кода под свои нужды

### Пример адаптации:

**Оригинальный код:**
```python
def analyze_csv(filename):
    df = pd.read_csv(filename)
    return df.describe()
```

**Адаптированный код:**
```python
def analyze_data(filename, file_type='csv'):
    """
    Анализ данных из файла.
    
    Адаптировано из: https://github.com/username/project
    """
    if file_type == 'csv':
        df = pd.read_csv(filename)
    elif file_type == 'excel':
        df = pd.read_excel(filename)
    else:
        raise ValueError(f"Неподдерживаемый тип: {file_type}")
    
    return df.describe()
```

## 📋 Шаг 7: Добавление в requirements.txt

Если используете как зависимость:

```txt
# requirements.txt
pandas>=1.5.0
numpy>=1.23.0

# Пакет с GitHub
git+https://github.com/username/package.git@main

# Или конкретная версия
git+https://github.com/username/package.git@v1.0.0
```

## 📋 Шаг 8: Практический пример

### Сценарий: Нужна функция для обработки данных

```bash
# 1. Найти подходящий проект на GitHub
# Например: https://github.com/username/data-processing

# 2. Клонировать
git clone https://github.com/username/data-processing.git
cd data-processing

# 3. Изучить код
cat src/process.py

# 4. Найти нужную функцию
grep -r "def clean_data" .

# 5. Изучить зависимости функции
# Посмотреть импорты в файле

# 6. Скопировать в свой проект
# Создать файл utils.py в своем проекте
```

**В вашем проекте:**
```python
# utils.py
"""
Утилиты для обработки данных
Часть кода адаптирована из: https://github.com/username/data-processing
Лицензия: MIT
"""

import pandas as pd

def clean_data(df):
    """
    Очистка данных.
    
    Адаптировано из: https://github.com/username/data-processing
    """
    # Удалить дубликаты
    df = df.drop_duplicates()
    
    # Удалить пустые значения
    df = df.dropna()
    
    return df
```

## 📋 Шаг 9: Обновление чужого кода

Если используете клонированный репозиторий:

```bash
# Получить обновления
git pull origin main

# Если установлен в режиме разработки, изменения сразу доступны
```

## 📋 Шаг 10: Лучшие практики

### ✅ Хорошие практики:

1. **Всегда указывайте источник:**
   ```python
   # Адаптировано из: https://github.com/username/project
   ```

2. **Проверяйте лицензию:**
   - Убедитесь, что можете использовать код
   - Сохраняйте информацию о лицензии

3. **Изучайте код перед использованием:**
   - Понимайте, что делает код
   - Проверяйте на ошибки

4. **Адаптируйте под свои нужды:**
   - Не копируйте слепо
   - Улучшайте и адаптируйте

### ❌ Плохие практики:

1. Копирование без указания источника
2. Использование без проверки лицензии
3. Копирование всего проекта, когда нужна только часть
4. Использование устаревшего кода

## ✅ Проверка знаний

1. Найдите полезный проект на GitHub
2. Проверьте лицензию
3. Определите, как лучше использовать (библиотека/копирование)
4. Используйте код в своем проекте
5. Укажите источник в комментариях

## 🎓 Что вы узнали

- ✅ Способы использования чужого кода
- ✅ Как проверить лицензию
- ✅ Как использовать как библиотеку
- ✅ Как копировать и адаптировать код
- ✅ Лучшие практики использования

## ➡️ Следующий шаг

Переходите к **Практике 4: Клонирование и изучение реального проекта**

---

**💡 Совет:** Не изобретайте велосипед! Используйте готовые решения, но всегда понимайте, что делает код, и адаптируйте под свои нужды!
