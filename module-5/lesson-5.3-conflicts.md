# Урок 5.3: Разрешение конфликтов (conflicts)

## 🎯 Цель урока
Научиться разрешать конфликты при слиянии веток и синхронизации с GitHub.

## 📚 Термины урока

**Merge Conflict** (EN) → **Конфликт слияния** (RU) → Ситуация, когда Git не может автоматически объединить изменения из разных веток.

**Conflict Marker** (EN) → **Маркер конфликта** (RU) → Специальные символы в файле, показывающие конфликтующие участки кода.

**Resolution** (EN) → **Разрешение** (RU) → Процесс выбора или объединения конфликтующих изменений.

## 📋 Шаг 1: Когда возникают конфликты?

**Конфликты возникают, когда:**
- Одна и та же строка изменена в разных ветках
- Файл удален в одной ветке, но изменен в другой
- При слиянии веток (merge)
- При получении изменений (pull)
- При применении изменений (rebase)

## 📋 Шаг 2: Пример возникновения конфликта

### Сценарий:

**Ветка main:**
```python
def calculate_mean(data):
    return sum(data) / len(data)
```

**Ветка feature:**
```python
def calculate_mean(numbers):
    return sum(numbers) / len(numbers) if numbers else 0
```

При слиянии Git не знает, какую версию выбрать → конфликт!

## 📋 Шаг 3: Попытка слияния с конфликтом

```bash
# Попытка слить ветку
git merge feature-branch
```

**Результат:**
```
Auto-merging main.py
CONFLICT (content): Merge conflict in main.py
Automatic merge failed; fix conflicts and then commit the result.
```

## 📋 Шаг 4: Понимание маркеров конфликта

Git добавляет в файл специальные маркеры:

```python
<<<<<<< HEAD
# Код из текущей ветки (main)
def calculate_mean(data):
    return sum(data) / len(data)
=======
# Код из сливаемой ветки (feature)
def calculate_mean(numbers):
    return sum(numbers) / len(numbers) if numbers else 0
>>>>>>> feature-branch
```

**Структура маркеров:**
- `<<<<<<< HEAD` — начало конфликта, код из текущей ветки
- `=======` — разделитель
- `>>>>>>> feature-branch` — конец конфликта, код из сливаемой ветки

## 📋 Шаг 5: Просмотр конфликтов

```bash
# Показать файлы с конфликтами
git status
```

**Результат:**
```
Unmerged paths:
  (use "git add <file>..." to mark as resolved)
        both modified:   main.py
```

### Просмотр конфликтов в Cursor:

1. Откройте файл с конфликтом
2. Cursor подсветит конфликтующие участки
3. Вы увидите опции:
   - **Accept Current Change** — принять текущую версию
   - **Accept Incoming Change** — принять входящую версию
   - **Accept Both Changes** — принять обе версии
   - **Compare Changes** — сравнить изменения

## 📋 Шаг 6: Разрешение конфликта вручную

### Шаг 6.1: Открыть файл с конфликтом

```bash
# Открыть в редакторе
code main.py
# Или в Cursor
```

### Шаг 6.2: Выбрать нужную версию

**Вариант 1: Оставить версию из main**
```python
def calculate_mean(data):
    return sum(data) / len(data)
```

**Вариант 2: Оставить версию из feature**
```python
def calculate_mean(numbers):
    return sum(numbers) / len(numbers) if numbers else 0
```

**Вариант 3: Объединить обе версии**
```python
def calculate_mean(numbers):
    """Вычисление среднего значения с проверкой на пустой список"""
    if not numbers:
        return 0
    return sum(numbers) / len(numbers)
```

### Шаг 6.3: Удалить маркеры конфликта

Удалите все маркеры:
- `<<<<<<< HEAD`
- `=======`
- `>>>>>>> feature-branch`

### Шаг 6.4: Сохранить файл

Сохраните файл с разрешенным конфликтом.

## 📋 Шаг 7: Завершение разрешения конфликта

```bash
# Добавить разрешенный файл
git add main.py

# Завершить слияние
git commit
# Или с сообщением
git commit -m "Разрешен конфликт в main.py"
```

**Git автоматически создаст коммит слияния.**

## 📋 Шаг 8: Разрешение через интерфейс Cursor

1. Откройте файл с конфликтом
2. Нажмите на кнопку разрешения конфликта:
   - **Accept Current Change** (принять текущую)
   - **Accept Incoming Change** (принять входящую)
   - **Accept Both Changes** (принять обе)
3. Сохраните файл
4. В Source Control нажмите "Stage" (добавить в staging)
5. Создайте коммит

## 📋 Шаг 9: Отмена слияния (если нужно)

Если конфликт слишком сложный и хотите начать заново:

```bash
# Отменить слияние
git merge --abort

# Вернуться к состоянию до слияния
```

## 📋 Шаг 10: Конфликты при pull

При `git pull` тоже могут быть конфликты:

```bash
# Получить изменения
git pull origin main
```

**Если конфликт:**
1. Разрешите конфликты (как описано выше)
2. Добавьте файлы: `git add .`
3. Завершите: `git commit`
4. Отправьте: `git push`

## 📋 Шаг 11: Предотвращение конфликтов

### Лучшие практики:

1. **Часто синхронизируйтесь:**
   ```bash
   git pull origin main  # перед началом работы
   ```

2. **Работайте в отдельных ветках:**
   - Каждая функция в своей ветке
   - Меньше пересечений изменений

3. **Общайтесь с командой:**
   - Кто работает над какими файлами
   - Избегайте одновременного редактирования

4. **Делайте маленькие коммиты:**
   - Проще разрешать конфликты
   - Понятнее, что изменилось

## 📋 Шаг 12: Практический пример

```bash
# 1. Создать ветку
git checkout -b feature-improvement

# 2. Изменить файл
# ... редактируете main.py ...

# 3. Закоммитить
git add main.py
git commit -m "Улучшена функция"

# 4. Вернуться на main
git checkout main

# 5. Изменить тот же файл
# ... редактируете main.py в другом месте ...

# 6. Закоммитить
git add main.py
git commit -m "Исправлена ошибка"

# 7. Попытка слияния
git merge feature-improvement

# 8. Конфликт! Разрешить:
# - Открыть main.py
# - Выбрать нужную версию или объединить
# - Удалить маркеры
# - Сохранить

# 9. Завершить слияние
git add main.py
git commit -m "Разрешен конфликт"
```

## ✅ Проверка знаний

1. Создайте конфликт намеренно (измените один файл в двух ветках)
2. Попробуйте слить ветки
3. Разрешите конфликт вручную
4. Разрешите конфликт через интерфейс Cursor
5. Завершите слияние

## 🎓 Что вы узнали

- ✅ Когда возникают конфликты
- ✅ Как читать маркеры конфликта
- ✅ Как разрешать конфликты вручную
- ✅ Как разрешать через интерфейс Cursor
- ✅ Как предотвращать конфликты

## ➡️ Следующий шаг

Переходите к **Уроку 5.4: Лучшие практики для аналитики данных**

---

**💡 Совет:** Конфликты — это нормально! Не паникуйте. Просто выберите нужную версию или объедините изменения. Cursor поможет визуально разрешить конфликты.
