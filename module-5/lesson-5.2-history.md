# Урок 5.2: Работа с историей коммитов

## 🎯 Цель урока
Научиться эффективно работать с историей коммитов: просматривать, искать, откатывать изменения.

## 📚 Термины урока

**Commit History** (EN) → **История коммитов** (RU) → Последовательность всех коммитов в репозитории.

**HEAD** (EN) → **Голова** (RU) → Указатель на текущий коммит. Обычно это последний коммит в текущей ветке.

**Reset** (EN) → **Сброс** (RU) → Откат к предыдущему состоянию. Команда: `git reset`.

## 📋 Шаг 1: Просмотр истории коммитов

### Базовый просмотр:

```bash
# Показать все коммиты
git log

# Краткий формат (одна строка)
git log --oneline

# С графиком веток
git log --oneline --graph

# С ограничением количества
git log -10  # последние 10 коммитов
```

### Расширенный просмотр:

```bash
# С статистикой изменений
git log --stat

# С полным diff
git log -p

# Поиск по автору
git log --author="Имя"

# Поиск по сообщению
git log --grep="ключевое слово"

# По дате
git log --since="2024-01-01"
git log --until="2024-12-31"
```

## 📋 Шаг 2: Просмотр конкретного коммита

```bash
# Показать последний коммит
git show

# Показать конкретный коммит
git show abc123

# Показать только изменения
git show abc123 --stat
```

**Что показывает:**
- Информация о коммите (автор, дата, сообщение)
- Список измененных файлов
- Сами изменения (diff)

## 📋 Шаг 3: Поиск в истории

### Поиск по содержимому:

```bash
# Найти коммиты, где изменялся файл
git log -- filename.py

# Найти коммиты с определенным текстом
git log -S "function_name"  # где добавлялась/удалялась функция

# Найти коммиты, затрагивающие функцию
git log -L :function_name:filename.py
```

### Поиск по сообщению:

```bash
# Найти коммиты с ключевым словом
git log --grep="bug fix"

# С учетом регистра
git log -i --grep="bug"  # -i = ignore case
```

## 📋 Шаг 4: Просмотр изменений между коммитами

```bash
# Изменения между двумя коммитами
git diff abc123 def456

# Изменения между коммитом и текущим состоянием
git diff abc123

# Изменения в конкретном файле
git diff abc123 def456 -- filename.py

# Изменения между ветками
git diff main..feature-branch
```

## 📋 Шаг 5: Навигация по истории

### Просмотр предыдущих версий:

```bash
# Переключиться на конкретный коммит (detached HEAD)
git checkout abc123

# Вернуться обратно
git checkout main

# Или использовать switch
git switch main
```

**Важно:** В режиме detached HEAD не создавайте коммиты (или создайте ветку).

## 📋 Шаг 6: Откат изменений

### Откат рабочей директории (незакоммиченные изменения):

```bash
# Отменить изменения в файле (опасно!)
git checkout -- filename.py

# Или (современный способ)
git restore filename.py

# Отменить все изменения
git restore .
```

**Внимание:** Это удалит незакоммиченные изменения!

### Откат staging area:

```bash
# Убрать файл из staging
git restore --staged filename.py

# Или
git reset HEAD filename.py
```

## 📋 Шаг 7: Откат коммитов (reset)

### Soft reset (сохраняет изменения в staging):

```bash
# Откатиться на 1 коммит назад, сохранив изменения
git reset --soft HEAD~1
```

### Mixed reset (сохраняет изменения в рабочей директории):

```bash
# Откатиться на 1 коммит назад
git reset HEAD~1
# Или
git reset --mixed HEAD~1
```

### Hard reset (удаляет все изменения):

```bash
# ОПАСНО! Откатиться и удалить все изменения
git reset --hard HEAD~1
```

**Внимание:** `--hard` удаляет изменения безвозвратно!

### Откат к конкретному коммиту:

```bash
# Откатиться к коммиту abc123
git reset --hard abc123
```

## 📋 Шаг 8: Отмена коммита (revert)

**Revert** создает новый коммит, отменяющий изменения:

```bash
# Создать коммит, отменяющий последний коммит
git revert HEAD

# Отменить конкретный коммит
git revert abc123
```

**Преимущество revert:**
- Не переписывает историю
- Безопасно для уже отправленных коммитов
- Создает новый коммит с отменой

## 📋 Шаг 9: Изменение последнего коммита

### Изменить сообщение:

```bash
# Изменить сообщение последнего коммита
git commit --amend -m "Новое сообщение"
```

### Добавить изменения в последний коммит:

```bash
# Внести изменения
# ... редактируете файлы ...

# Добавить в staging
git add .

# Добавить в последний коммит
git commit --amend --no-edit  # --no-edit = не менять сообщение
```

**Важно:** Не изменяйте коммиты, которые уже отправлены на GitHub (если работаете в команде)!

## 📋 Шаг 10: Практические примеры

### Пример 1: Найти, когда была добавлена функция

```bash
# Найти коммиты с функцией
git log -S "def calculate_mean" --oneline

# Показать изменения
git show abc123
```

### Пример 2: Откатить последний коммит (локально)

```bash
# Откатить, сохранив изменения
git reset HEAD~1

# Или откатить полностью
git reset --hard HEAD~1
```

### Пример 3: Отменить коммит, который уже отправлен

```bash
# Создать коммит-отмену
git revert HEAD

# Отправить на GitHub
git push origin main
```

## ✅ Проверка знаний

1. Просмотрите историю коммитов с графиком
2. Найдите коммит по ключевому слову
3. Просмотрите изменения в конкретном коммите
4. Попробуйте откатить последний коммит (soft reset)
5. Измените сообщение последнего коммита

## 🎓 Что вы узнали

- ✅ Как просматривать историю коммитов
- ✅ Как искать в истории
- ✅ Как просматривать изменения
- ✅ Как откатывать изменения
- ✅ Разницу между reset и revert

## ➡️ Следующий шаг

Переходите к **Уроку 5.3: Разрешение конфликтов (conflicts)**

---

**💡 Совет:** Будьте осторожны с `git reset --hard` — он удаляет изменения безвозвратно! Всегда проверяйте статус перед выполнением опасных команд.
