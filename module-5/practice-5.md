# Практика 5: Настройка проекта аналитики данных

## 🎯 Цель практики
Создать полноценный проект аналитики данных с правильной структурой, .gitignore, документацией и настройками для работы с Git.

## 📋 Задание 1: Создание структуры проекта

Создайте структуру проекта:

```bash
# Создать папку проекта
mkdir data-analysis-project
cd data-analysis-project

# Создать структуру папок
mkdir data\raw
mkdir data\processed
mkdir notebooks
mkdir src
mkdir outputs
mkdir config
```

**Итоговая структура:**
```
data-analysis-project/
├── data/
│   ├── raw/
│   └── processed/
├── notebooks/
├── src/
├── outputs/
└── config/
```

## 📋 Задание 2: Инициализация Git-репозитория

```bash
# Инициализировать Git
git init

# Проверить статус
git status
```

## 📋 Задание 3: Создание .gitignore

Создайте файл `.gitignore` с правилами для аналитики данных:

```gitignore
# Python
__pycache__/
*.py[cod]
*$py.class
venv/
env/
ENV/

# Данные
*.csv
*.xlsx
*.xls
*.parquet
*.h5
*.hdf5
data/
raw_data/
processed_data/

# Результаты
outputs/
results/
plots/
*.png
*.jpg
*.pdf

# Jupyter Notebook
.ipynb_checkpoints
*.ipynb_checkpoints

# Временные файлы
*.tmp
*.temp
temp/
tmp/

# Секреты
.env
.secrets
config/secrets.yaml

# IDE
.vscode/
.idea/
*.swp
```

**Проверка:**
```bash
# Создать тестовый файл данных
echo "test data" > data/raw/test.csv

# Проверить статус
git status
# test.csv не должен появиться в списке!
```

## 📋 Задание 4: Создание основных файлов

### 4.1. README.md

```markdown
# Анализ данных: [Название проекта]

## Описание
[Опишите, что анализируете]

## Данные
Данные для проекта находятся в `data/raw/`.

**Источник данных:** [укажите источник или как получить данные]

## Установка

```bash
# Создать виртуальное окружение
python -m venv venv

# Активировать (Windows)
venv\Scripts\activate

# Установить зависимости
pip install -r requirements.txt
```

## Использование

```bash
# Запустить анализ
python src/analysis.py
```

## Структура проекта

```
data-analysis-project/
├── data/          # Данные (не в Git)
├── notebooks/      # Jupyter notebooks
├── src/           # Исходный код
├── outputs/       # Результаты (не в Git)
└── config/        # Конфигурация
```

## Результаты
Результаты анализа находятся в `outputs/`.

## Автор
[Ваше имя]
```

### 4.2. requirements.txt

```txt
pandas==2.0.3
numpy==1.24.3
matplotlib==3.7.2
seaborn==0.12.2
jupyter==1.0.0
```

### 4.3. src/data_loading.py

```python
"""
Модуль для загрузки данных
"""
import pandas as pd
import os

def load_data(filename, data_dir="data/raw"):
    """
    Загрузка данных из файла.
    
    Parameters:
    -----------
    filename : str
        Имя файла
    data_dir : str
        Папка с данными
        
    Returns:
    --------
    pd.DataFrame
        Загруженные данные
    """
    filepath = os.path.join(data_dir, filename)
    
    if not os.path.exists(filepath):
        raise FileNotFoundError(f"Файл {filepath} не найден")
    
    # Определить тип файла по расширению
    if filename.endswith('.csv'):
        return pd.read_csv(filepath)
    elif filename.endswith('.xlsx') or filename.endswith('.xls'):
        return pd.read_csv(filepath)
    else:
        raise ValueError(f"Неподдерживаемый формат файла: {filename}")

def save_data(df, filename, output_dir="data/processed"):
    """
    Сохранение данных в файл.
    
    Parameters:
    -----------
    df : pd.DataFrame
        Данные для сохранения
    filename : str
        Имя файла
    output_dir : str
        Папка для сохранения
    """
    os.makedirs(output_dir, exist_ok=True)
    filepath = os.path.join(output_dir, filename)
    
    if filename.endswith('.csv'):
        df.to_csv(filepath, index=False)
    elif filename.endswith('.xlsx'):
        df.to_excel(filepath, index=False)
    else:
        raise ValueError(f"Неподдерживаемый формат файла: {filename}")
```

### 4.4. src/analysis.py

```python
"""
Модуль для анализа данных
"""
import pandas as pd
import numpy as np
from data_loading import load_data, save_data

def calculate_statistics(df):
    """
    Вычисление базовой статистики.
    
    Parameters:
    -----------
    df : pd.DataFrame
        Данные для анализа
        
    Returns:
    --------
    pd.DataFrame
        Статистика
    """
    return df.describe()

def analyze_data(input_file, output_file=None):
    """
    Основная функция анализа.
    
    Parameters:
    -----------
    input_file : str
        Имя входного файла
    output_file : str, optional
        Имя файла для сохранения результатов
    """
    # Загрузить данные
    print(f"Загрузка данных из {input_file}...")
    df = load_data(input_file)
    
    # Вычислить статистику
    print("Вычисление статистики...")
    stats = calculate_statistics(df)
    
    print("\nБазовая статистика:")
    print(stats)
    
    # Сохранить результаты (если указано)
    if output_file:
        save_data(stats, output_file)
        print(f"\nРезультаты сохранены в {output_file}")
    
    return stats

if __name__ == "__main__":
    # Пример использования
    # analyze_data("dataset.csv", "statistics.csv")
    print("Модуль анализа данных")
    print("Используйте: analyze_data('filename.csv')")
```

### 4.5. config/settings.yaml.template

```yaml
# Шаблон конфигурации
# Скопируйте в settings.yaml и заполните

data:
  input_path: "data/raw/dataset.csv"
  output_path: "data/processed/"

analysis:
  random_seed: 42
  test_size: 0.2
```

## 📋 Задание 5: Создание первого коммита

```bash
# Добавить все файлы
git add .

# Проверить, что добавлено
git status

# Создать первый коммит
git commit -m "Initial commit: создана структура проекта аналитики данных"
```

**Проверка:**
```bash
# Убедиться, что данные НЕ закоммичены
git ls-files | findstr "\.csv"
# Не должно быть вывода!

# Убедиться, что код закоммичен
git ls-files | findstr "\.py"
# Должны быть файлы из src/
```

## 📋 Задание 6: Создание репозитория на GitHub

1. Создайте репозиторий на GitHub
2. Свяжите локальный репозиторий с GitHub
3. Отправьте код

```bash
# Добавить remote
git remote add origin https://github.com/username/data-analysis-project.git

# Отправить код
git push -u origin main
```

## 📋 Задание 7: Создание Jupyter Notebook

Создайте `notebooks/01-data-exploration.ipynb`:

```python
# Ячейка 1: Импорты
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import sys
import os

# Добавить src в путь
sys.path.append('../src')
from data_loading import load_data

# Ячейка 2: Загрузка данных
# df = load_data('dataset.csv')
# df.head()

# Ячейка 3: Исследование данных
# df.info()
# df.describe()
```

**Важно:** Очистите выводы перед коммитом!

## 📋 Задание 8: Настройка воспроизводимости

### 8.1. Обновить requirements.txt с точными версиями

```txt
pandas==2.0.3
numpy==1.24.3
matplotlib==3.7.2
seaborn==0.12.2
jupyter==1.0.0
```

### 8.2. Добавить seed в код

В `src/analysis.py`:
```python
import numpy as np
np.random.seed(42)  # Для воспроизводимости
```

### 8.3. Обновить README с информацией об окружении

```markdown
## Окружение

- Python 3.10+
- Операционная система: Windows 10
- См. requirements.txt для зависимостей
```

## 📋 Задание 9: Создание ветки для анализа

```bash
# Создать ветку для нового анализа
git checkout -b analysis-exploratory

# Работать в ветке
# ... вносите изменения ...

# Коммитить
git add .
git commit -m "Добавлен exploratory анализ"

# Отправить ветку
git push -u origin analysis-exploratory
```

## 📋 Задание 10: Финальная проверка

Проверьте структуру проекта:

```bash
# Показать структуру
tree /F
# Или
dir /s

# Проверить, что в Git
git ls-files

# Проверить, что НЕ в Git (должно быть пусто)
git status
```

**Ожидаемый результат:**
- ✅ Структура проекта создана
- ✅ .gitignore настроен правильно
- ✅ Данные и результаты НЕ в Git
- ✅ Код и документация в Git
- ✅ README содержит инструкции
- ✅ requirements.txt с версиями

## ✅ Критерии успешного выполнения

- ✅ Создана правильная структура проекта
- ✅ .gitignore настроен для аналитики данных
- ✅ Данные и результаты не коммитятся
- ✅ Код и документация в Git
- ✅ README содержит все необходимые инструкции
- ✅ requirements.txt с точными версиями
- ✅ Проект на GitHub

## 🎓 Что вы закрепили

- ✅ Структуру проекта аналитики данных
- ✅ Настройку .gitignore
- ✅ Работу с данными в Git
- ✅ Обеспечение воспроизводимости
- ✅ Документацию проекта

## ➡️ Следующий шаг

Поздравляю! Вы завершили курс обучения GitHub! 🎉

Теперь вы можете:
- ✅ Создавать и управлять репозиториями
- ✅ Работать с ветками
- ✅ Изучать чужие проекты
- ✅ Правильно настраивать проекты аналитики данных

**Продолжайте практиковаться на реальных проектах!**

---

**💡 Совет:** Используйте этот проект как шаблон для будущих проектов аналитики данных!
