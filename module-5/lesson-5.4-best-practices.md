# Урок 5.4: Лучшие практики для аналитики данных

## 🎯 Цель урока
Изучить специфические практики работы с Git для проектов аналитики данных: работа с данными, ноутбуками, результатами.

## 📚 Термины урока

**Data Versioning** (EN) → **Версионирование данных** (RU) → Отслеживание версий наборов данных (обычно через DVC или Git LFS).

**Notebook Workflow** (EN) → **Workflow ноутбуков** (RU) → Процесс работы с Jupyter Notebook в Git.

**Reproducibility** (EN) → **Воспроизводимость** (RU) → Возможность воспроизвести результаты анализа.

## 📋 Шаг 1: Структура проекта аналитики данных

### Рекомендуемая структура:

```
data-analysis-project/
├── README.md
├── requirements.txt
├── .gitignore
├── data/
│   ├── raw/          # Исходные данные (в .gitignore)
│   └── processed/    # Обработанные данные (в .gitignore)
├── notebooks/        # Jupyter notebooks
│   ├── 01-data-exploration.ipynb
│   ├── 02-analysis.ipynb
│   └── 03-visualization.ipynb
├── src/              # Код для переиспользования
│   ├── data_loading.py
│   ├── analysis.py
│   └── visualization.py
├── outputs/          # Результаты (в .gitignore)
│   ├── plots/
│   └── reports/
└── config/           # Конфигурация
    └── settings.yaml
```

## 📋 Шаг 2: .gitignore для аналитики данных

```gitignore
# Python
__pycache__/
*.py[cod]
venv/
env/

# Данные (ВАЖНО!)
*.csv
*.xlsx
*.xls
*.parquet
*.h5
*.hdf5
*.feather
data/
raw_data/
processed_data/
datasets/

# Результаты
outputs/
results/
plots/
figures/
*.png
*.jpg
*.pdf
reports/

# Jupyter Notebook
.ipynb_checkpoints
*.ipynb_checkpoints

# Временные файлы
*.tmp
*.temp
temp/
tmp/

# Секреты
.env
.secrets
config/secrets.yaml
*.key

# Большие файлы
*.zip
*.tar.gz
*.rar
```

## 📋 Шаг 3: Работа с Jupyter Notebooks

### Проблемы с ноутбуками в Git:

- Много метаданных (выводы, метрики выполнения)
- Конфликты при слиянии (JSON формат)
- Большой размер файлов

### Решения:

**Вариант 1: Очистка перед коммитом**

Используйте `nbstripout`:

```bash
# Установить
pip install nbstripout

# Настроить для проекта
nbstripout --install

# Теперь при коммите выводы автоматически удаляются
```

**Вариант 2: Ручная очистка**

В Jupyter: `Edit → Clear All Outputs` перед коммитом

**Вариант 3: Хранить только код**

Экспортировать в `.py` файлы для версионирования:

```bash
# В Jupyter
File → Download as → Python (.py)
```

## 📋 Шаг 4: Версионирование данных

### Проблема:

Данные обычно большие и не должны быть в Git.

### Решения:

**Вариант 1: Описание данных в README**

```markdown
## Данные

Данные для проекта находятся в:
- `data/raw/` — исходные данные
- Источник: [ссылка на источник данных]

Для запуска проекта:
1. Скачайте данные по ссылке
2. Поместите в `data/raw/`
```

**Вариант 2: DVC (Data Version Control)**

Для серьезных проектов:

```bash
# Установить DVC
pip install dvc

# Инициализировать
dvc init

# Добавить данные
dvc add data/raw/dataset.csv

# Коммитить .dvc файл (не сам dataset.csv)
git add data/raw/dataset.csv.dvc
git commit -m "Добавлен dataset"
```

**Вариант 3: Git LFS (Large File Storage)**

Для больших файлов:

```bash
# Установить Git LFS
git lfs install

# Отслеживать большие файлы
git lfs track "*.csv"
git lfs track "data/**"
```

## 📋 Шаг 5: Работа с результатами

### Результаты не должны быть в Git:

- Графики можно пересоздать
- Отчеты генерируются автоматически
- Занимают много места

### Решение:

```gitignore
# Результаты
outputs/
results/
plots/
*.png
*.jpg
*.pdf
```

**Но сохраняйте код для генерации результатов!**

## 📋 Шаг 6: Воспроизводимость (Reproducibility)

### Что нужно для воспроизводимости:

1. **Точные версии зависимостей:**

```txt
# requirements.txt с версиями
pandas==1.5.3
numpy==1.23.5
matplotlib==3.6.2
```

2. **Описание окружения:**

```markdown
## Окружение

- Python 3.10
- Операционная система: Windows 10
- См. requirements.txt для зависимостей
```

3. **Инструкции по запуску:**

```markdown
## Запуск

1. Установить зависимости: `pip install -r requirements.txt`
2. Поместить данные в `data/raw/`
3. Запустить: `python src/analysis.py`
```

4. **Сохранение случайных seed:**

```python
# В начале скрипта
import numpy as np
np.random.seed(42)  # Для воспроизводимости
```

## 📋 Шаг 7: Коммиты для аналитики данных

### Хорошие сообщения коммитов:

```
Добавлен скрипт загрузки данных
Исправлена ошибка в расчете статистики
Обновлен анализ с новыми данными
Добавлена визуализация распределений
Улучшена обработка пропущенных значений
```

### Структура коммитов:

1. **Подготовка данных:**
   ```
   Добавлен скрипт очистки данных
   Добавлена функция обработки выбросов
   ```

2. **Анализ:**
   ```
   Добавлен анализ корреляций
   Реализован расчет статистик
   ```

3. **Визуализация:**
   ```
   Добавлены графики распределений
   Создана визуализация временных рядов
   ```

## 📋 Шаг 8: Работа с конфигурацией

### Хранить конфигурацию в Git:

```yaml
# config/settings.yaml
data:
  input_path: "data/raw/dataset.csv"
  output_path: "data/processed/"

analysis:
  random_seed: 42
  test_size: 0.2
```

### Не хранить секреты:

```gitignore
# Секреты
config/secrets.yaml
.env
```

**Создайте шаблон:**
```yaml
# config/settings.yaml.template
data:
  api_key: "YOUR_API_KEY_HERE"
```

## 📋 Шаг 9: Документация проекта

### README для аналитики данных:

```markdown
# Анализ данных: [Название проекта]

## Описание
[Что анализируем]

## Данные
[Откуда данные, как получить]

## Установка
```bash
pip install -r requirements.txt
```

## Использование
```bash
python src/analysis.py
```

## Результаты
[Где найти результаты, как их интерпретировать]

## Структура проекта
[Описание структуры]
```

## 📋 Шаг 10: Workflow для аналитики данных

### Типичный workflow:

```bash
# 1. Начать новый анализ
git checkout -b analysis-new-dataset

# 2. Работать над анализом
# ... редактируете notebooks, скрипты ...

# 3. Коммитить часто
git add notebooks/01-exploration.ipynb
git commit -m "Добавлен первичный анализ данных"

# 4. Продолжать работу
git add src/analysis.py
git commit -m "Реализован расчет метрик"

# 5. Слить в main
git checkout main
git merge analysis-new-dataset
git push origin main
```

## ✅ Проверка знаний

1. Создайте структуру проекта аналитики данных
2. Настройте .gitignore
3. Добавьте README с инструкциями
4. Создайте requirements.txt с версиями
5. Сделайте первый коммит

## 🎓 Что вы узнали

- ✅ Структуру проекта аналитики данных
- ✅ Настройку .gitignore для данных
- ✅ Работу с Jupyter Notebooks
- ✅ Обеспечение воспроизводимости
- ✅ Workflow для аналитики

## ➡️ Следующий шаг

Переходите к **Практике 5: Настройка проекта аналитики данных**

---

**💡 Совет:** Всегда документируйте, откуда данные и как их получить. Это критично для воспроизводимости анализа!
