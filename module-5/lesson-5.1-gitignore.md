# Урок 5.1: .gitignore для разных типов проектов

## 🎯 Цель урока
Научиться правильно настраивать .gitignore для Python-проектов, проектов аналитики данных и веб-приложений.

## 📚 Термины урока

**Gitignore** (EN) → **Игнорирование файлов** (RU) → Файл `.gitignore`, который указывает Git, какие файлы не нужно отслеживать.

**Untracked Files** (EN) → **Неотслеживаемые файлы** (RU) → Файлы, которые Git видит, но не отслеживает (из-за .gitignore или потому что не добавлены).

## 📋 Шаг 1: Зачем нужен .gitignore?

**Проблемы без .gitignore:**
- Коммитятся личные данные (пароли, API ключи)
- Коммитятся большие файлы данных
- Коммитятся временные файлы
- Коммитятся файлы IDE

**Решение:** `.gitignore` — файл, который говорит Git игнорировать определенные файлы и папки.

## 📋 Шаг 2: Создание .gitignore

```bash
# Создать файл .gitignore
touch .gitignore
# Или в Windows
type nul > .gitignore
```

**Или через Cursor:**
1. Создайте новый файл `.gitignore`
2. Добавьте правила

## 📋 Шаг 3: Синтаксис .gitignore

### Базовые правила:

```
# Игнорировать конкретный файл
secret.txt

# Игнорировать все файлы с расширением
*.log
*.tmp

# Игнорировать папку
venv/
data/

# Игнорировать файлы в любой папке
**/temp/

# Исключение (не игнорировать)
!important.log
```

### Специальные символы:

- `*` — любое количество символов
- `?` — один символ
- `**` — любая вложенность папок
- `!` — исключение (не игнорировать)
- `#` — комментарий

## 📋 Шаг 4: .gitignore для Python-проектов

```gitignore
# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# PyInstaller
*.manifest
*.spec

# Unit test / coverage reports
htmlcov/
.tox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
.hypothesis/
.pytest_cache/

# Virtual environments
venv/
env/
ENV/
env.bak/
venv.bak/

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
```

## 📋 Шаг 5: .gitignore для проектов аналитики данных

```gitignore
# Python (из предыдущего раздела)
__pycache__/
*.py[cod]
venv/
env/

# Данные (ВАЖНО!)
*.csv
*.xlsx
*.xls
*.parquet
*.h5
*.hdf5
*.feather
data/
raw_data/
processed_data/
datasets/

# Результаты анализа
outputs/
results/
plots/
figures/
*.png
*.jpg
*.pdf
reports/

# Jupyter Notebook
.ipynb_checkpoints
*.ipynb_checkpoints

# Временные файлы
*.tmp
*.temp
temp/
tmp/

# Конфигурация с секретами
config/secrets.yaml
.env
.secrets

# Большие файлы
*.zip
*.tar.gz
*.rar
```

**Важно для аналитики данных:**
- Не коммитьте данные (особенно большие файлы)
- Не коммитьте результаты (можно пересоздать)
- Не коммитьте секреты (API ключи, пароли)

## 📋 Шаг 6: .gitignore для веб-приложений

```gitignore
# Python
__pycache__/
*.py[cod]
venv/
env/

# Environment variables
.env
.env.local
.env.*.local

# Dependencies
node_modules/
package-lock.json

# Build outputs
dist/
build/
*.egg-info/

# Logs
*.log
logs/

# Database
*.db
*.sqlite
*.sqlite3

# Static files (если генерируются)
staticfiles/
media/

# IDE
.vscode/
.idea/
```

## 📋 Шаг 7: Проверка .gitignore

```bash
# Проверить статус
git status

# Должны быть видны только нужные файлы
# Игнорируемые файлы не появятся в списке
```

### Проверить, что файл игнорируется:

```bash
# Проверить, игнорируется ли файл
git check-ignore -v filename.txt
```

**Результат:**
```
.gitignore:5:*.txt    filename.txt
```

Это означает, что файл игнорируется правилом в строке 5 файла `.gitignore`.

## 📋 Шаг 8: Игнорирование уже отслеживаемых файлов

Если файл уже был закоммичен, но теперь нужно игнорировать:

```bash
# Удалить из Git (но оставить локально)
git rm --cached filename.txt

# Добавить в .gitignore
echo "filename.txt" >> .gitignore

# Закоммитить
git add .gitignore
git commit -m "Добавлен filename.txt в .gitignore"
```

## 📋 Шаг 9: Готовые шаблоны .gitignore

GitHub предоставляет готовые шаблоны:

1. Откройте https://github.com/github/gitignore
2. Найдите нужный шаблон (например, `Python.gitignore`)
3. Скопируйте содержимое
4. Вставьте в ваш `.gitignore`

**Или используйте генератор:**
- https://www.toptal.com/developers/gitignore

## 📋 Шаг 10: Практический пример

```bash
# 1. Создать .gitignore
cat > .gitignore << EOF
# Python
__pycache__/
*.py[cod]
venv/

# Данные
*.csv
data/

# Результаты
outputs/
*.png
EOF

# 2. Проверить
git status

# 3. Если нужно, добавить исключение
echo "!important_data.csv" >> .gitignore
```

## ✅ Проверка знаний

1. Создайте `.gitignore` для Python-проекта
2. Добавьте правила для данных и результатов
3. Проверьте, что файлы игнорируются
4. Попробуйте добавить исключение для важного файла

## 🎓 Что вы узнали

- ✅ Зачем нужен .gitignore
- ✅ Синтаксис .gitignore
- ✅ Правила для разных типов проектов
- ✅ Как проверить, что файл игнорируется
- ✅ Как игнорировать уже отслеживаемые файлы

## ➡️ Следующий шаг

Переходите к **Уроку 5.2: Работа с историей коммитов**

---

**💡 Совет:** Всегда создавайте .gitignore в начале проекта, чтобы не закоммитить лишние файлы!
